---
phase: 01.2-add-all-shadcn-components
plan: 01
type: execute
---

<objective>
Install all Shadcn/ui components needed for the REOS real estate platform.

Purpose: Pre-install the complete component library to avoid piecemeal installation during feature development. Having all components available enables faster iteration on authentication, profiles, marketplace, and dashboard features.
Output: Full Shadcn component library installed in src/components/ui/, ready for use across all future phases.
</objective>

<execution_context>
~/.claude/get-shit-done/workflows/execute-phase.md
~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-foundation/01-01-SUMMARY.md
@.planning/phases/01-foundation/01-02-SUMMARY.md

**Current state:**
- 4 Shadcn components installed: Button, Card, Input, Label
- Shadcn configured with new-york style, neutral base, hugeicons
- Small radius (0.25rem) for compact/dense design

**Components needed for REOS features:**

| Feature Area | Required Components |
|--------------|---------------------|
| Authentication | Dialog, Form, Alert, Toast |
| Profiles | Avatar, Badge, Tabs, Textarea, Select, Switch |
| Marketplace | Table, Pagination, Skeleton, Tooltip, Sheet |
| Dashboard | Chart, Progress, Separator, Dropdown Menu |
| General | Sidebar, Navigation Menu, Breadcrumb, Command |

**Installation approach:**
Use `npx shadcn@latest add --all --yes` to install all available components at once.
This ensures complete coverage and avoids future installation interruptions.
</context>

<tasks>

<task type="auto">
  <name>Task 1: Install all Shadcn components</name>
  <files>src/components/ui/*.tsx, package.json</files>
  <action>
1. Run the Shadcn CLI to add all components:
   ```bash
   npx shadcn@latest add --all --yes
   ```

2. This will install all available Shadcn components including:
   - Layout: Sidebar, Navigation Menu, Breadcrumb, Separator, Scroll Area, Resizable
   - Forms: Form, Select, Checkbox, Radio Group, Switch, Textarea, Slider, Input OTP, Combobox
   - Feedback: Alert, Toast (Sonner), Progress, Skeleton, Spinner
   - Overlays: Dialog, Sheet, Drawer, Popover, Tooltip, Hover Card, Context Menu, Dropdown Menu
   - Data Display: Table, Avatar, Badge, Calendar, Chart, Accordion, Collapsible, Tabs, Carousel
   - Navigation: Menubar, Command, Pagination, Toggle, Toggle Group

3. Accept any dependency installations (Radix UI primitives, etc.)

4. Verify all components are installed:
   ```bash
   ls src/components/ui/ | wc -l
   ```
   Expected: 40+ component files
  </action>
  <verify>
- ls src/components/ui/ shows 40+ files
- No TypeScript errors: npm run build succeeds
- package.json has new @radix-ui/* dependencies
  </verify>
  <done>
- All Shadcn components installed in src/components/ui/
- Build passes without errors
- Dependencies properly installed
  </done>
</task>

<task type="auto">
  <name>Task 2: Verify critical components for upcoming phases</name>
  <files>src/components/ui/*.tsx</files>
  <action>
1. Verify these critical components exist (needed for Phase 2 Authentication):
   - dialog.tsx
   - form.tsx
   - alert.tsx
   - toast.tsx or sonner.tsx

2. Verify these components exist (needed for Phase 3 Profiles):
   - avatar.tsx
   - badge.tsx
   - tabs.tsx
   - textarea.tsx
   - select.tsx
   - switch.tsx

3. Verify these components exist (needed for Phase 4 Marketplace):
   - table.tsx
   - pagination.tsx
   - skeleton.tsx
   - tooltip.tsx
   - sheet.tsx

4. Run a quick type check:
   ```bash
   npx tsc --noEmit
   ```

5. Ensure the dev server starts without errors:
   ```bash
   npm run dev &
   sleep 5
   curl -s http://localhost:3000 > /dev/null && echo "Dev server OK"
   ```
  </action>
  <verify>
- All critical component files exist
- npx tsc --noEmit passes (no type errors)
- npm run dev starts successfully
  </verify>
  <done>
- All 15+ critical components verified present
- No TypeScript errors
- Dev server runs successfully
  </done>
</task>

</tasks>

<verification>
Before declaring phase complete:
- [ ] `ls src/components/ui/ | wc -l` shows 40+ component files
- [ ] `npm run build` succeeds without errors
- [ ] Critical components for Phases 2-4 are all present
- [ ] No TypeScript errors from new components
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- No TypeScript or build errors
- Full Shadcn component library available
- Ready for Phase 2: Authentication
</success_criteria>

<output>
After completion, create `.planning/phases/01.2-add-all-shadcn-components/01.2-01-SUMMARY.md`:

# Phase 1.2 Plan 1: Shadcn Components Summary

**[One-liner summarizing what shipped]**

## Accomplishments

- [Key outcome 1]
- [Key outcome 2]

## Files Created/Modified

- `path/to/file.ts` - Description

## Decisions Made

[Key decisions and rationale, or "None"]

## Issues Encountered

[Problems and resolutions, or "None"]

## Next Phase Readiness

Ready for Phase 2: Authentication (Clerk integration)
</output>
