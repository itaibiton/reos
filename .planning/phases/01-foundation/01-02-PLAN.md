---
phase: 01-foundation
plan: 02
type: execute
---

<objective>
Create the base application layout with header, sidebar, and main content area.

Purpose: Establish the visual shell that all pages will use, providing consistent navigation and structure.
Output: Responsive app shell with placeholder navigation ready for auth integration.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-phase.md
@~/.claude/get-shit-done/templates/summary.md
@~/.claude/get-shit-done/references/checkpoints.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-foundation/01-01-SUMMARY.md

**Tech available (from Plan 01-01):**
- Next.js App Router with TypeScript
- Tailwind CSS configured
- Shadcn/ui: Button, Card, Input, Label
- Convex connected

**User types (from PROJECT.md):**
- Investors: find properties, connect with providers, track deals
- Service Providers: brokers, mortgage advisors, lawyers

**Layout needs:**
- Header: Logo, navigation, user menu placeholder
- Sidebar: Role-based navigation (will vary by user type in Phase 2)
- Main content area: Where pages render
- Mobile responsive: Collapsible sidebar on small screens
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create app shell layout components</name>
  <files>src/components/layout/Header.tsx, src/components/layout/Sidebar.tsx, src/components/layout/AppShell.tsx, src/app/layout.tsx</files>
  <action>
1. Create src/components/layout/Header.tsx:
   - Fixed top header with logo "REOS" on left
   - Placeholder for user menu on right (just a Button for now)
   - Use Tailwind: h-16, border-b, flex items-center justify-between, px-4
   - Mobile: hamburger menu button (hidden on desktop) to toggle sidebar

2. Create src/components/layout/Sidebar.tsx:
   - Left sidebar with navigation links
   - Placeholder links: Dashboard, Properties, Deals, Settings
   - Use Tailwind: w-64, border-r, fixed left-0, top-16, bottom-0
   - Mobile: absolute overlay, hidden by default, shown via state
   - Accept `isOpen` and `onClose` props for mobile toggle

3. Create src/components/layout/AppShell.tsx:
   - Combines Header + Sidebar + main content area
   - Uses React state for mobile sidebar toggle
   - Main content: ml-64 on desktop (sidebar width), ml-0 on mobile
   - Padding: p-6 for content area
   - "use client" for state management

4. Update src/app/layout.tsx:
   - Wrap {children} in AppShell component
   - Keep ConvexClientProvider as outer wrapper

5. Update src/app/page.tsx:
   - Show welcome content with Card component
   - "Welcome to REOS" heading
   - Brief description from PROJECT.md core value

Do NOT add actual navigation logic yet - Phase 2 (auth) will determine role-based nav.
Use placeholder navigation items as visual scaffolding.
  </action>
  <verify>npm run dev shows header, sidebar, and main content area</verify>
  <done>AppShell renders with header, sidebar, main area; mobile hamburger toggles sidebar</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>Responsive app shell with header, sidebar, and main content</what-built>
  <how-to-verify>
    1. Run: npm run dev
    2. Visit: http://localhost:3000
    3. Desktop (>1024px):
       - Header spans full width at top
       - Sidebar visible on left (w-64)
       - Main content fills remaining space
       - "Welcome to REOS" card visible
    4. Mobile (resize to <768px):
       - Sidebar hidden by default
       - Hamburger menu visible in header
       - Click hamburger: sidebar overlays content
       - Click outside or X: sidebar closes
    5. Confirm: No layout shift, no horizontal scroll
  </how-to-verify>
  <resume-signal>Type "approved" to continue, or describe issues to fix</resume-signal>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `npm run build` succeeds
- [ ] Layout renders correctly on desktop
- [ ] Mobile sidebar toggle works
- [ ] No TypeScript errors
- [ ] Human verification approved
</verification>

<success_criteria>
- All tasks completed
- AppShell provides consistent layout structure
- Responsive design works on mobile and desktop
- Phase 1: Foundation complete
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation/01-02-SUMMARY.md` following summary template.

Include "Phase 1 complete, ready for Phase 2: Authentication" in Next Step section.
</output>
