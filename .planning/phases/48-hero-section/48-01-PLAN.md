---
phase: 48-hero-section
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/landing/Hero/Hero.tsx
  - messages/en.json
  - messages/he.json
autonomous: true
user_setup: []

must_haves:
  truths:
    - "Hero section fills full viewport (100vh) with headline visible above fold"
    - "Subheadline clearly explains REOS connects US investors with Israeli properties"
    - "Primary CTA button scrolls to #contact section when clicked"
    - "Scroll indicator animates to guide users to continue scrolling"
  artifacts:
    - path: "src/components/landing/Hero/Hero.tsx"
      provides: "Hero section with id attribute, investor-focused content, CTA to #contact"
      contains: "id=\"hero\""
    - path: "messages/en.json"
      provides: "English hero translations for investor audience"
      contains: "Israeli Real Estate"
    - path: "messages/he.json"
      provides: "Hebrew hero translations for investor audience"
      contains: "נדל\"ן ישראלי"
  key_links:
    - from: "src/components/landing/Hero/Hero.tsx"
      to: "#contact"
      via: "Primary CTA href"
      pattern: "href=\"#contact\""
    - from: "LandingNav section links"
      to: "Hero section"
      via: "id attribute"
      pattern: "id=\"hero\""
---

<objective>
Update the Hero section for v1.7 investor-focused landing page with compelling headline, value proposition subheadline, and CTA that scrolls to contact form.

Purpose: Hero is the first thing visitors see - must immediately communicate REOS's value to US investors looking at Israeli real estate opportunities.

Output: Updated Hero component with investor-focused content, proper id attribute for hash navigation, and CTA scrolling to #contact section.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/48-hero-section/48-RESEARCH.md
@.planning/phases/47-landing-page-foundation/47-01-SUMMARY.md
@src/components/landing/Hero/Hero.tsx
@messages/en.json (landing.hero section)
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update Hero component structure for v1.7</name>
  <files>src/components/landing/Hero/Hero.tsx</files>
  <action>
Update Hero.tsx with the following changes:

1. Add `id="hero"` attribute to the section element for hash navigation:
   ```tsx
   <section
     ref={ref}
     id="hero"
     className={cn(...)}
   >
   ```

2. Add `min-h-[100vh]` as fallback alongside `min-h-dvh` for older browsers:
   ```tsx
   "relative min-h-dvh min-h-[100vh]",
   ```

3. Change primary CTA from Link to sign-up page to anchor to #contact:
   - Change: `<Link href={`/${locale}/sign-up`}>`
   - To: `<a href="#contact">`
   - Remove the unused `locale` import from useLocale if no longer needed

4. Keep secondary CTA but update to scroll to #features since video doesn't exist yet:
   - Change: Button with no action
   - To: `<a href="#features">` with "Learn More" text (translation key change in Task 2)
   - Keep the Play icon but could swap to ArrowDown or similar (optional)

5. Update trust metrics to be investor-relevant (will use translations):
   - Keep 3-stat layout
   - Stats will use new translation keys: trustDeals, trustROI, trustInvestors

All existing animations, reduced motion support, and accessibility features must be preserved.
  </action>
  <verify>
    - `grep -n 'id="hero"' src/components/landing/Hero/Hero.tsx` returns a match
    - `grep -n 'href="#contact"' src/components/landing/Hero/Hero.tsx` returns a match for primary CTA
    - `grep -n 'min-h-dvh min-h-\[100vh\]' src/components/landing/Hero/Hero.tsx` confirms fallback
    - TypeScript compiles: `npx tsc --noEmit`
  </verify>
  <done>Hero section has id="hero", primary CTA scrolls to #contact, viewport height has fallback</done>
</task>

<task type="auto">
  <name>Task 2: Update English hero translations for investor focus</name>
  <files>messages/en.json</files>
  <action>
Update the landing.hero section in messages/en.json with investor-focused messaging:

```json
"hero": {
  "badge": "US Investors \u2194 Israeli Real Estate",
  "headline": "Your Gateway to Israeli Property Investment",
  "subheadline": "Connect, Track, Close Deals with Confidence",
  "description": "REOS connects US investors with premium Israeli real estate opportunities. From discovery to closing, manage your entire investment journey on one platform.",
  "ctaPrimary": "Get Started",
  "ctaSecondary": "Learn More",
  "trustLabel": "Trusted by investors nationwide",
  "trustDeals": "Deals Closed",
  "trustROI": "Avg. ROI",
  "trustInvestors": "Active Investors"
}
```

Key changes:
- badge: Changed from property management to US-Israel investment focus
- headline: Clear value proposition for investors
- subheadline: Action-oriented tagline about the journey
- description: Explains what REOS does for investors
- ctaPrimary: "Get Started" (matches NAV-CTA decision)
- ctaSecondary: "Learn More" (scrolls to features since video not ready)
- Trust metrics: Changed to investor-relevant metrics (Deals, ROI, Investors)

Note: The \u2194 is a bidirectional arrow character that may need to render as "US Investors <-> Israeli Real Estate" or similar.
  </action>
  <verify>
    - `grep -n "Israeli" messages/en.json` shows the new investor-focused content
    - `grep -n "trustDeals" messages/en.json` confirms new trust metric keys
    - JSON is valid: `node -e "require('./messages/en.json')"`
  </verify>
  <done>English translations updated with investor-focused headline, subheadline, description, and trust metrics</done>
</task>

<task type="auto">
  <name>Task 3: Update Hebrew hero translations for investor focus</name>
  <files>messages/he.json</files>
  <action>
Update the landing.hero section in messages/he.json with Hebrew investor-focused messaging:

```json
"hero": {
  "badge": "משקיעים אמריקאים \u2194 נדל\"ן ישראלי",
  "headline": "השער שלך להשקעות נדל\"ן בישראל",
  "subheadline": "התחבר, עקוב, סגור עסקאות בביטחון",
  "description": "REOS מחברת משקיעים אמריקאים עם הזדמנויות נדל\"ן מובחרות בישראל. מגילוי ועד סגירה, נהל את כל מסע ההשקעה שלך בפלטפורמה אחת.",
  "ctaPrimary": "התחל עכשיו",
  "ctaSecondary": "למד עוד",
  "trustLabel": "בהאמון משקיעים ברחבי הארץ",
  "trustDeals": "עסקאות שנסגרו",
  "trustROI": "תשואה ממוצעת",
  "trustInvestors": "משקיעים פעילים"
}
```

Key translations:
- badge: "US Investors <-> Israeli Real Estate" in Hebrew
- headline: "Your gateway to Israeli real estate investments"
- subheadline: "Connect, track, close deals with confidence" in Hebrew
- description: Full Hebrew translation of investor value proposition
- Trust metrics: Hebrew equivalents for deals, ROI, investors
  </action>
  <verify>
    - `grep -n "ישראלי" messages/he.json` shows Hebrew investor content
    - `grep -n "trustDeals" messages/he.json` confirms new trust metric keys
    - JSON is valid: `node -e "require('./messages/he.json')"`
  </verify>
  <done>Hebrew translations updated with investor-focused messaging matching English content</done>
</task>

</tasks>

<verification>
After all tasks complete:

1. **Visual verification** - Run `npm run dev`, visit http://localhost:3000
   - Hero fills viewport (100vh)
   - Investor-focused headline visible: "Your Gateway to Israeli Property Investment"
   - Subheadline: "Connect, Track, Close Deals with Confidence"
   - Primary CTA shows "Get Started"
   - Trust metrics show investor-relevant stats

2. **Navigation verification**
   - Click "Get Started" CTA -> scrolls to #contact section
   - Click "Learn More" -> scrolls to #features section
   - Navbar "Hero" link (if exists) scrolls to hero section

3. **Animation verification**
   - Scroll indicator bounces at bottom
   - Hero visual (ecosystem) animates on load
   - Text reveals with stagger animation

4. **Accessibility**
   - Check reduced motion: `prefers-reduced-motion: reduce` disables animations
   - `aria-label="Hero section"` present on section
</verification>

<success_criteria>
- Hero section has `id="hero"` attribute for hash navigation
- Hero fills 100vh viewport with `min-h-dvh min-h-[100vh]` fallback
- Headline clearly communicates investor value proposition
- Subheadline explains US investors + Israeli properties connection
- Primary CTA "Get Started" scrolls to #contact (not sign-up page)
- Trust metrics show investor-relevant data (Deals, ROI, Investors)
- All existing animations and accessibility features preserved
- TypeScript compiles without errors
</success_criteria>

<output>
After completion, create `.planning/phases/48-hero-section/48-01-SUMMARY.md`
</output>
