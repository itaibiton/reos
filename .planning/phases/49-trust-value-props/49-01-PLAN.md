---
phase: 49-trust-value-props
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/landing/TrustSection/TrustSection.tsx
  - src/components/landing/TrustSection/LogoGrid.tsx
  - src/components/landing/TrustSection/index.ts
  - src/components/landing/ValueProps/ValuePropsSection.tsx
  - src/components/landing/ValueProps/ValuePropCard.tsx
  - src/components/landing/ValueProps/index.ts
  - messages/en.json
  - messages/he.json
  - src/app/[locale]/(main)/page.tsx
autonomous: true

must_haves:
  truths:
    - "User sees 'Trusted by' or 'Featured in' heading with partner logos"
    - "User sees 4 value propositions with distinct icons"
    - "Each value proposition has brief, scannable description"
    - "Both sections animate into view on scroll"
  artifacts:
    - path: "src/components/landing/TrustSection/TrustSection.tsx"
      provides: "Trust section wrapper with heading"
      exports: ["TrustSection"]
    - path: "src/components/landing/TrustSection/LogoGrid.tsx"
      provides: "Logo grid with grayscale hover effect"
      exports: ["LogoGrid"]
    - path: "src/components/landing/ValueProps/ValuePropsSection.tsx"
      provides: "Value props section with 4 cards"
      exports: ["ValuePropsSection"]
    - path: "src/components/landing/ValueProps/ValuePropCard.tsx"
      provides: "Individual value prop card with icon"
      exports: ["ValuePropCard"]
  key_links:
    - from: "src/app/[locale]/(main)/page.tsx"
      to: "TrustSection, ValuePropsSection"
      via: "import and render after Hero"
      pattern: "import.*TrustSection|import.*ValuePropsSection"
    - from: "TrustSection.tsx"
      to: "messages/*.json"
      via: "useTranslations('landing.trust')"
      pattern: "useTranslations.*landing\\.trust"
    - from: "ValuePropsSection.tsx"
      to: "messages/*.json"
      via: "useTranslations('landing.valueProps')"
      pattern: "useTranslations.*landing\\.valueProps"
---

<objective>
Create Trust and Value Props sections for the landing page

Purpose: Show social proof via partner logos and communicate key platform benefits to convert visitors
Output: TrustSection and ValuePropsSection components integrated into landing page with scroll animations
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/49-trust-value-props/49-RESEARCH.md
@src/components/landing/shared/SectionWrapper.tsx
@src/components/landing/shared/animations.ts
@src/components/landing/Features/FeatureCard.tsx
@src/app/[locale]/(main)/page.tsx
@messages/en.json
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create TrustSection components</name>
  <files>
    src/components/landing/TrustSection/TrustSection.tsx
    src/components/landing/TrustSection/LogoGrid.tsx
    src/components/landing/TrustSection/index.ts
  </files>
  <action>
Create the TrustSection module:

**TrustSection.tsx:**
- "use client" directive
- Import SectionWrapper, SectionHeader from "../shared"
- Import LogoGrid from "./LogoGrid"
- Import useTranslations from "next-intl"
- Use `id="trust"` for hash navigation
- Use `background="muted"` for subtle contrast
- Use `animate={true}` for scroll-triggered fade-in
- Call `useTranslations("landing.trust")` for heading/subheading

**LogoGrid.tsx:**
- "use client" directive
- Import motion from "framer-motion", fadeInUp from "../shared/animations"
- Define placeholder logos array (6 items) with name and placeholder SVG data URLs
- Create responsive grid: `grid-cols-2 sm:grid-cols-3 md:grid-cols-6`
- Each logo: `grayscale opacity-60 hover:grayscale-0 hover:opacity-100 transition-all duration-300`
- Wrap each in motion.div with fadeInUp variants
- Use placeholder SVG rectangles with text (e.g., "Partner 1") until real logos available
- Add comment: `// TODO: Replace with real partner/media logos`

**index.ts:**
- Export TrustSection and LogoGrid
  </action>
  <verify>
- Files exist at correct paths
- TypeScript compiles: `npx tsc --noEmit`
- No lint errors in new files
  </verify>
  <done>TrustSection component renders heading, subheading, and 6 placeholder logos in a responsive grid with grayscale hover effect</done>
</task>

<task type="auto">
  <name>Task 2: Create ValueProps components</name>
  <files>
    src/components/landing/ValueProps/ValuePropsSection.tsx
    src/components/landing/ValueProps/ValuePropCard.tsx
    src/components/landing/ValueProps/index.ts
  </files>
  <action>
Create the ValueProps module:

**ValuePropsSection.tsx:**
- "use client" directive
- Import SectionWrapper, SectionHeader from "../shared"
- Import ValuePropCard from "./ValuePropCard"
- Import useTranslations from "next-intl"
- Import icons from lucide-react: Globe, BarChart3, Users, ShieldCheck
- Use `id="value-props"` for potential hash navigation
- Use `background="transparent"` to contrast with trust section
- Use `animate={true}` for scroll-triggered stagger
- Define icon mapping: { network: Globe, tracking: BarChart3, partners: Users, security: ShieldCheck }
- Get items from `t.raw("items")` as array of { key, icon, title, description }
- Render responsive grid: `grid-cols-1 sm:grid-cols-2 lg:grid-cols-4 gap-8`
- Map items to ValuePropCard components

**ValuePropCard.tsx:**
- "use client" directive
- Import motion from "framer-motion", type LucideIcon from "lucide-react"
- Import fadeInUp from "../shared/animations"
- Import cn from "@/lib/utils"
- Props: { icon: LucideIcon, title: string, description: string }
- Layout: flex-col items-center text-center
- Icon container: w-16 h-16 mb-4 rounded-xl bg-landing-primary/10 flex items-center justify-center
- Icon: w-8 h-8 text-landing-primary
- Title: text-xl font-semibold mb-2 text-landing-text
- Description: text-muted-foreground leading-relaxed text-sm
- Wrap in motion.div with fadeInUp variants

**index.ts:**
- Export ValuePropsSection and ValuePropCard
  </action>
  <verify>
- Files exist at correct paths
- TypeScript compiles: `npx tsc --noEmit`
- Icons imported correctly from lucide-react
  </verify>
  <done>ValuePropsSection component renders 4 value prop cards with icons in responsive grid with scroll-triggered stagger animation</done>
</task>

<task type="auto">
  <name>Task 3: Add translations and integrate into landing page</name>
  <files>
    messages/en.json
    messages/he.json
    src/app/[locale]/(main)/page.tsx
  </files>
  <action>
**messages/en.json** - Add after "hero" section in "landing" object:
```json
"trust": {
  "heading": "Trusted By Industry Leaders",
  "subheading": "Join hundreds of investors who trust REOS for their Israeli real estate investments"
},
"valueProps": {
  "heading": "Why Choose REOS",
  "subheading": "Everything you need to invest in Israeli real estate with confidence",
  "items": [
    {
      "key": "network",
      "icon": "network",
      "title": "Global Network",
      "description": "Connect with 50+ verified Israeli partners including agents, lawyers, and accountants"
    },
    {
      "key": "tracking",
      "icon": "tracking",
      "title": "Deal Tracking",
      "description": "Follow every deal from initial interest to close with our 7-stage pipeline"
    },
    {
      "key": "partners",
      "icon": "partners",
      "title": "Vetted Partners",
      "description": "Work only with pre-screened, English-speaking service providers"
    },
    {
      "key": "security",
      "icon": "security",
      "title": "Secure Platform",
      "description": "End-to-end encrypted communications and document storage"
    }
  ]
}
```

**messages/he.json** - Add Hebrew translations:
```json
"trust": {
  "heading": "מהימנים על ידי מובילי התעשייה",
  "subheading": "הצטרפו למאות משקיעים שסומכים על REOS להשקעות הנדל\"ן שלהם בישראל"
},
"valueProps": {
  "heading": "למה לבחור ב-REOS",
  "subheading": "כל מה שצריך להשקעה בנדל\"ן ישראלי בביטחון",
  "items": [
    {
      "key": "network",
      "icon": "network",
      "title": "רשת גלובלית",
      "description": "התחברו עם 50+ שותפים ישראליים מאומתים כולל סוכנים, עורכי דין ורואי חשבון"
    },
    {
      "key": "tracking",
      "icon": "tracking",
      "title": "מעקב עסקאות",
      "description": "עקבו אחר כל עסקה מהתעניינות ראשונית ועד סגירה עם צינור 7 השלבים שלנו"
    },
    {
      "key": "partners",
      "icon": "partners",
      "title": "שותפים מאומתים",
      "description": "עבדו רק עם נותני שירות שעברו סינון מקדים ודוברי אנגלית"
    },
    {
      "key": "security",
      "icon": "security",
      "title": "פלטפורמה מאובטחת",
      "description": "תקשורת ואחסון מסמכים מוצפנים מקצה לקצה"
    }
  ]
}
```

**page.tsx:**
1. Add imports: `import { TrustSection } from "@/components/landing/TrustSection";` and `import { ValuePropsSection } from "@/components/landing/ValueProps";`
2. Insert TrustSection after Hero, before ServicesGrid
3. Insert ValuePropsSection after TrustSection, before ServicesGrid
4. Add GeometricDivider between ValuePropsSection and ServicesGrid if needed for visual separation

Result order: Hero -> TrustSection -> ValuePropsSection -> ServicesGrid -> ...
  </action>
  <verify>
- JSON files are valid: `node -e "require('./messages/en.json')"` and `node -e "require('./messages/he.json')"`
- Dev server runs without errors: `npm run dev` (check for translation key errors)
- Visit http://localhost:3000 - Trust and Value sections visible after hero
- Scroll down - both sections fade in with animation
  </verify>
  <done>Landing page shows Trust section with logos, Value Props with 4 cards, both animate on scroll, translations work in en and he locales</done>
</task>

</tasks>

<verification>
1. All 6 new component files exist and export correctly
2. TypeScript compiles without errors
3. Landing page renders Trust and Value sections in correct order
4. Scroll-triggered animations work (sections fade in when scrolled into view)
5. Grayscale hover effect works on logos
6. Responsive layouts work on mobile (1-2 cols) and desktop (4-6 cols)
7. Hebrew locale displays correct translations with RTL layout
</verification>

<success_criteria>
- "Trusted By Industry Leaders" heading visible with 6 placeholder logos
- 4 value proposition cards visible with distinct icons (Globe, BarChart3, Users, ShieldCheck)
- Each value prop has title and brief description
- Both sections animate on scroll (fade-in effect)
- No TypeScript or runtime errors
</success_criteria>

<output>
After completion, create `.planning/phases/49-trust-value-props/49-01-SUMMARY.md`
</output>
