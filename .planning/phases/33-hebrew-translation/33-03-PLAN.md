---
phase: 33-hebrew-translation
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - messages/he.json
autonomous: true

must_haves:
  truths:
    - "User sees Hebrew property listing titles and descriptions"
    - "User sees Hebrew property form labels and validation messages"
    - "User sees Hebrew empty states and error messages for properties"
  artifacts:
    - path: "messages/he.json"
      provides: "properties namespace Hebrew translations"
      contains: "properties"
  key_links:
    - from: "messages/he.json"
      to: "useTranslations('properties')"
      via: "next-intl message loading"
      pattern: "\"properties\""
---

<objective>
Translate properties namespace to Hebrew.

Purpose: Enable Hebrew-speaking users to browse, view, and manage property listings in Hebrew.
Output: messages/he.json with properties namespace (118 keys) fully translated.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/33-hebrew-translation/33-RESEARCH.md
@messages/en.json
@messages/he.json
</context>

<tasks>

<task type="auto">
  <name>Task 1: Translate properties core and card keys (40 keys)</name>
  <files>messages/he.json</files>
  <action>
Add Hebrew translations for properties core, count, and card sections to messages/he.json.

Keys to translate:
- properties.title, properties.browseTitle, properties.savedTitle, properties.listingsTitle
- properties.newTitle, properties.editTitle, properties.newDescription, properties.editDescription
- properties.count (ICU plural)
- properties.listingsCount (ICU plural)
- properties.card.* (8 keys: bedrooms, bathrooms, size, roi, cap, rent, perMonth)

CRITICAL - Preserve ICU plural syntax:
```json
"count": "{count, plural, =0 {לא נמצאו נכסים} =1 {נכס אחד} other {# נכסים}}"
"listingsCount": "{count, plural, =0 {0 נכסים} =1 {נכס אחד} other {# נכסים}}"
```

Domain-specific terms:
- "Browse Properties" = "עיון בנכסים"
- "Saved Properties" = "נכסים שמורים"
- "Your Listings" = "הנכסים שלך"
- "ROI" = "תשואה"
- "Cap Rate" = "שיעור היוון"
  </action>
  <verify>
Run: `node -e "const he=require('./messages/he.json'); console.log('properties.card:', he.properties?.card ? 'EXISTS' : 'MISSING')"`
  </verify>
  <done>properties core, count, and card sections translated with correct ICU plural syntax</done>
</task>

<task type="auto">
  <name>Task 2: Translate properties details, form, and remaining sections (78 keys)</name>
  <files>messages/he.json</files>
  <action>
Add Hebrew translations for properties details, form, empty, save, saved, neighborhood, and amenities sections.

Keys to translate:
- properties.details.* (22 keys: description, amenities, location, investment, propertyDetails, etc.)
- properties.form.* (33 keys: title, description, price, address, city, propertyType, etc.)
- properties.empty.* (12 keys: noProperties, noPropertiesFiltered, clearFilters, etc.)
- properties.save.* (4 keys)
- properties.saved.* (4 keys including nested empty object)
- properties.neighborhood.* (13 keys)
- properties.amenities.* (1 key)

Preserve ICU syntax in saved.count:
```json
"count": "{count} {count, plural, =1 {נכס שמור} other {נכסים שמורים}}"
```

Domain-specific terms:
- "Start Deal" = "התחל עסקה"
- "Investment Metrics" = "מדדי השקעה"
- "Expected ROI" = "תשואה צפויה"
- "Monthly Rent" = "שכירות חודשית"
- "Neighborhood Info" = "מידע על השכונה"
  </action>
  <verify>
Run: `node -e "const he=require('./messages/he.json'); const keys = ['details','form','empty','save','saved','neighborhood','amenities']; keys.forEach(k => console.log('properties.' + k + ':', he.properties?.[k] ? 'EXISTS' : 'MISSING'))"`
  </verify>
  <done>All properties sub-sections translated with 118 total keys</done>
</task>

</tasks>

<verification>
After both tasks:
```bash
node -e "
const he=require('./messages/he.json');
const en=require('./messages/en.json');
function countKeys(obj) {
  let count = 0;
  for (const key in obj) {
    if (typeof obj[key] === 'object' && !Array.isArray(obj[key])) {
      count += countKeys(obj[key]);
    } else { count++; }
  }
  return count;
}
const propEn = countKeys(en.properties);
const propHe = countKeys(he.properties || {});
console.log('properties: ' + propHe + '/' + propEn + ' keys');
console.log(propHe === propEn ? 'PASS' : 'INCOMPLETE');
"
```
</verification>

<success_criteria>
- [ ] properties namespace: 118/118 keys translated
- [ ] All ICU plural patterns preserved correctly
- [ ] No JSON syntax errors
- [ ] Property type terms match common namespace (residential, commercial, etc.)
</success_criteria>

<output>
After completion, create `.planning/phases/33-hebrew-translation/33-03-SUMMARY.md`
</output>
