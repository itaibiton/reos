---
phase: 33-hebrew-translation
plan: 05
type: execute
wave: 1
depends_on: []
files_modified:
  - messages/he.json
autonomous: true

must_haves:
  truths:
    - "User sees Hebrew provider card labels (rating, reviews, service areas)"
    - "User sees Hebrew provider profile sections"
    - "User sees Hebrew provider filtering options"
  artifacts:
    - path: "messages/he.json"
      provides: "providers namespace Hebrew translations"
      contains: "providers"
  key_links:
    - from: "messages/he.json"
      to: "useTranslations('providers')"
      via: "next-intl message loading"
      pattern: "\"providers\""
---

<objective>
Translate providers namespace to Hebrew.

Purpose: Enable Hebrew-speaking users to discover and view service providers in Hebrew.
Output: messages/he.json with providers namespace (41 keys) fully translated.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/33-hebrew-translation/33-RESEARCH.md
@messages/en.json
@messages/he.json
</context>

<tasks>

<task type="auto">
  <name>Task 1: Translate providers namespace (41 keys)</name>
  <files>messages/he.json</files>
  <action>
Add Hebrew translations for the entire "providers" namespace to messages/he.json.

Keys to translate:
- providers.title, providers.findProviders, providers.description
- providers.tabs.* (4 keys: all, brokers, mortgageAdvisors, lawyers)
- providers.card.* (12 keys including ICU plural reviews)
- providers.profile.* (12 keys)
- providers.filters.* (4 keys)
- providers.empty.* (4 keys)
- providers.notFound.* (2 keys)

CRITICAL - Preserve ICU plural syntax:
```json
"reviews": "{count, plural, =1 {ביקורת אחת} other {# ביקורות}}"
```

Domain-specific terms:
- "Service Providers" = "נותני שירות"
- "Brokers" = "מתווכים"
- "Mortgage Advisors" = "יועצי משכנתא"
- "Lawyers" = "עורכי דין"
- "Service Areas" = "אזורי שירות"
- "Specializations" = "התמחויות"
- "Years Experience" = "שנות ניסיון"
- "Deals Completed" = "עסקאות שהושלמו"
  </action>
  <verify>
Run: `node -e "
const he=require('./messages/he.json');
const en=require('./messages/en.json');
function countKeys(obj) {
  let count = 0;
  for (const key in obj) {
    if (typeof obj[key] === 'object' && !Array.isArray(obj[key])) {
      count += countKeys(obj[key]);
    } else { count++; }
  }
  return count;
}
const provEn = countKeys(en.providers);
const provHe = countKeys(he.providers || {});
console.log('providers: ' + provHe + '/' + provEn + ' keys');
console.log(provHe === provEn ? 'PASS' : 'INCOMPLETE');
"`
  </verify>
  <done>providers namespace exists in he.json with all 41 keys translated to Hebrew</done>
</task>

</tasks>

<verification>
```bash
node -e "
const he=require('./messages/he.json');
const sections = ['tabs', 'card', 'profile', 'filters', 'empty', 'notFound'];
sections.forEach(s => console.log('providers.' + s + ':', he.providers?.[s] ? 'EXISTS' : 'MISSING'));
"
```
</verification>

<success_criteria>
- [ ] providers namespace: 41/41 keys translated
- [ ] ICU plural for reviews preserved correctly
- [ ] Role names match common.roles (broker, mortgageAdvisor, lawyer)
- [ ] No JSON syntax errors
</success_criteria>

<output>
After completion, create `.planning/phases/33-hebrew-translation/33-05-SUMMARY.md`
</output>
