---
phase: 33-hebrew-translation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - messages/he.json
autonomous: true

must_haves:
  truths:
    - "User sees Hebrew dashboard title and welcome message"
    - "User sees Hebrew stats labels (Active Deals, Total Value, etc.)"
    - "User sees Hebrew analytics descriptions and metrics"
  artifacts:
    - path: "messages/he.json"
      provides: "dashboard and analytics Hebrew translations"
      contains: "dashboard"
  key_links:
    - from: "messages/he.json"
      to: "useTranslations('dashboard')"
      via: "next-intl message loading"
      pattern: "\"dashboard\""
---

<objective>
Translate dashboard and analytics namespaces to Hebrew.

Purpose: Enable Hebrew-speaking users to see dashboard and analytics UI in their language.
Output: messages/he.json with dashboard (54 keys) and analytics (25 keys) Hebrew translations.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/33-hebrew-translation/33-RESEARCH.md
@messages/en.json
@messages/he.json
</context>

<tasks>

<task type="auto">
  <name>Task 1: Translate dashboard namespace (54 keys)</name>
  <files>messages/he.json</files>
  <action>
Add Hebrew translations for the entire "dashboard" namespace to messages/he.json.

Keys to translate:
- dashboard.title, dashboard.welcome, dashboard.welcomeGeneric, dashboard.adminDashboard, dashboard.platformOverview
- dashboard.stats.* (12 keys: title, activeDeals, completedDeals, totalValue, avgRoi, cities, etc.)
- dashboard.sections.* (8 keys: recentDeals, myActiveDeals, pendingRequests, etc.)
- dashboard.trends.* (4 keys)
- dashboard.quickActions.* (5 keys)
- dashboard.profile.* (7 keys)
- dashboard.empty.* (6 keys)
- dashboard.requests.* (6 keys)

Preserve all ICU syntax:
- Keep {name} variables unchanged: "welcome": "ברוך שובך, {name}"

Use existing terminology from common namespace:
- "Active" = "פעיל"
- "Pending" = "ממתין"
- "Completed" = "הושלם"

Domain-specific terms:
- "Dashboard" = "לוח בקרה"
- "ROI" = "תשואה"
- "Total Value" = "סך ערך"
  </action>
  <verify>
Run: `node -e "const he=require('./messages/he.json'); console.log('dashboard keys:', Object.keys(he.dashboard || {}).length > 0 ? 'EXISTS' : 'MISSING')"`
  </verify>
  <done>dashboard namespace exists in he.json with all 54 keys translated to Hebrew</done>
</task>

<task type="auto">
  <name>Task 2: Translate analytics namespace (25 keys)</name>
  <files>messages/he.json</files>
  <action>
Add Hebrew translations for the entire "analytics" namespace to messages/he.json.

Keys to translate:
- analytics.title, analytics.description
- analytics.stats.* (6 keys: completedDeals, activeDeals, conversionRate, avgRating, noReviews, reviews, requestsOf)
- analytics.dealValue.* (4 keys)
- analytics.performance.* (5 keys)
- analytics.trends.* (4 keys)
- analytics.empty.* (2 keys)

Preserve ICU syntax:
- "{count} reviews" = "{count} ביקורות"
- "{accepted} of {total} requests" = "{accepted} מתוך {total} בקשות"

Domain-specific terms:
- "Conversion Rate" = "שיעור המרה"
- "Average Response Time" = "זמן תגובה ממוצע"
- "Deal Value" = "ערך עסקה"
  </action>
  <verify>
Run: `node -e "const he=require('./messages/he.json'); console.log('analytics keys:', Object.keys(he.analytics || {}).length > 0 ? 'EXISTS' : 'MISSING')"`
  </verify>
  <done>analytics namespace exists in he.json with all 25 keys translated to Hebrew</done>
</task>

</tasks>

<verification>
After both tasks:
```bash
node -e "
const he=require('./messages/he.json');
const en=require('./messages/en.json');
function countKeys(obj) {
  let count = 0;
  for (const key in obj) {
    if (typeof obj[key] === 'object' && !Array.isArray(obj[key])) {
      count += countKeys(obj[key]);
    } else { count++; }
  }
  return count;
}
const dashEn = countKeys(en.dashboard);
const dashHe = countKeys(he.dashboard || {});
const analEn = countKeys(en.analytics);
const analHe = countKeys(he.analytics || {});
console.log('dashboard: ' + dashHe + '/' + dashEn + ' keys');
console.log('analytics: ' + analHe + '/' + analEn + ' keys');
console.log(dashHe === dashEn && analHe === analEn ? 'PASS' : 'INCOMPLETE');
"
```
</verification>

<success_criteria>
- [ ] dashboard namespace: 54/54 keys translated
- [ ] analytics namespace: 25/25 keys translated
- [ ] All ICU patterns preserved ({name}, {count}, etc.)
- [ ] No JSON syntax errors (file parses correctly)
</success_criteria>

<output>
After completion, create `.planning/phases/33-hebrew-translation/33-01-SUMMARY.md`
</output>
