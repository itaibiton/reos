---
phase: 33-hebrew-translation
plan: 07
type: execute
wave: 1
depends_on: []
files_modified:
  - messages/he.json
autonomous: true

must_haves:
  truths:
    - "User sees Hebrew search results and suggestions"
    - "User sees Hebrew client management labels"
    - "User sees Hebrew error messages and coming soon pages"
  artifacts:
    - path: "messages/he.json"
      provides: "search, clients, misc Hebrew translations"
      contains: "search"
  key_links:
    - from: "messages/he.json"
      to: "useTranslations('search')"
      via: "next-intl message loading"
      pattern: "\"search\""
---

<objective>
Translate search, clients, and misc namespaces to Hebrew.

Purpose: Complete the Hebrew translation of all remaining namespaces.
Output: messages/he.json with search (32 keys), clients (58 keys), and misc (6 keys) translated.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/33-hebrew-translation/33-RESEARCH.md
@messages/en.json
@messages/he.json
</context>

<tasks>

<task type="auto">
  <name>Task 1: Translate search namespace (32 keys)</name>
  <files>messages/he.json</files>
  <action>
Add Hebrew translations for the entire "search" namespace to messages/he.json.

Keys to translate:
- search.title, search.placeholder, search.recentSearches, search.clearRecent, search.searchFor, search.enterTerm
- search.tabs.* (4 keys: all, users, posts, properties)
- search.results.* (6 keys)
- search.quickActions.* (2 keys)
- search.trending.* (6 keys)
- search.suggestions.* (6 keys)

Preserve ICU syntax:
- "Showing results for \"{query}\"" = "תוצאות עבור \"{query}\""
- "No results found for \"{query}\"" = "לא נמצאו תוצאות עבור \"{query}\""
- "Go to {page}" = "עבור אל {page}"

Domain-specific terms:
- "Search Results" = "תוצאות חיפוש"
- "Recent Searches" = "חיפושים אחרונים"
- "Trending" = "פופולרי"
- "Suggestions" = "הצעות"
- "People to Follow" = "אנשים לעקוב"
  </action>
  <verify>
Run: `node -e "const he=require('./messages/he.json'); console.log('search:', Object.keys(he.search || {}).length > 0 ? 'EXISTS' : 'MISSING')"`
  </verify>
  <done>search namespace exists in he.json with all 32 keys translated</done>
</task>

<task type="auto">
  <name>Task 2: Translate clients namespace (58 keys)</name>
  <files>messages/he.json</files>
  <action>
Add Hebrew translations for the entire "clients" namespace to messages/he.json.

Keys to translate:
- clients.title, clients.myClients, clients.clientCount, clients.clientCountPlural, clients.searchPlaceholder, clients.noMatch, clients.clearSearch
- clients.list.* (11 keys)
- clients.detail.* (15 keys)
- clients.questionnaire.* (17 keys)
- clients.empty.* (2 keys)
- clients.access.* (5 keys)

Preserve ICU syntax:
- "{count} client" = "{count} לקוח"
- "{count} clients" = "{count} לקוחות"
- "No clients match \"{query}\"" = "אין לקוחות התואמים ל\"{query}\""

Domain-specific terms:
- "Clients" = "לקוחות"
- "Active Deals" = "עסקאות פעילות"
- "Last Activity" = "פעילות אחרונה"
- "Investment Profile" = "פרופיל השקעות"
- "Provider Access Only" = "גישה לנותני שירות בלבד"
  </action>
  <verify>
Run: `node -e "const he=require('./messages/he.json'); console.log('clients:', Object.keys(he.clients || {}).length > 0 ? 'EXISTS' : 'MISSING')"`
  </verify>
  <done>clients namespace exists in he.json with all 58 keys translated</done>
</task>

<task type="auto">
  <name>Task 3: Translate misc namespace (6 keys)</name>
  <files>messages/he.json</files>
  <action>
Add Hebrew translations for the entire "misc" namespace to messages/he.json.

Keys to translate:
- misc.comingSoon.* (2 keys: title, description)
- misc.error.* (4 keys: notFound, unauthorized, generic, goBack)

Domain-specific terms:
- "Coming Soon" = "בקרוב"
- "Page not found" = "הדף לא נמצא"
- "You don't have access to this page" = "אין לך גישה לדף זה"
- "Something went wrong" = "משהו השתבש"
- "Go Back" = "חזור"
  </action>
  <verify>
Run: `node -e "const he=require('./messages/he.json'); console.log('misc:', Object.keys(he.misc || {}).length > 0 ? 'EXISTS' : 'MISSING')"`
  </verify>
  <done>misc namespace exists in he.json with all 6 keys translated</done>
</task>

</tasks>

<verification>
After all tasks:
```bash
node -e "
const he=require('./messages/he.json');
const en=require('./messages/en.json');
function countKeys(obj) {
  let count = 0;
  for (const key in obj) {
    if (typeof obj[key] === 'object' && !Array.isArray(obj[key])) {
      count += countKeys(obj[key]);
    } else { count++; }
  }
  return count;
}
const searchEn = countKeys(en.search);
const searchHe = countKeys(he.search || {});
const clientsEn = countKeys(en.clients);
const clientsHe = countKeys(he.clients || {});
const miscEn = countKeys(en.misc);
const miscHe = countKeys(he.misc || {});
console.log('search: ' + searchHe + '/' + searchEn + ' keys');
console.log('clients: ' + clientsHe + '/' + clientsEn + ' keys');
console.log('misc: ' + miscHe + '/' + miscEn + ' keys');
console.log(searchHe === searchEn && clientsHe === clientsEn && miscHe === miscEn ? 'PASS' : 'INCOMPLETE');
"
```
</verification>

<success_criteria>
- [ ] search namespace: 32/32 keys translated
- [ ] clients namespace: 58/58 keys translated
- [ ] misc namespace: 6/6 keys translated
- [ ] All ICU patterns preserved
- [ ] No JSON syntax errors
</success_criteria>

<output>
After completion, create `.planning/phases/33-hebrew-translation/33-07-SUMMARY.md`
</output>
