---
phase: 33-hebrew-translation
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - messages/he.json
autonomous: true

must_haves:
  truths:
    - "User sees Hebrew settings page titles and tab labels"
    - "User sees Hebrew profile form labels and placeholders"
    - "User sees Hebrew notification preference descriptions"
  artifacts:
    - path: "messages/he.json"
      provides: "settings and profile Hebrew translations"
      contains: "settings"
  key_links:
    - from: "messages/he.json"
      to: "useTranslations('settings')"
      via: "next-intl message loading"
      pattern: "\"settings\""
---

<objective>
Translate settings and profile namespaces to Hebrew.

Purpose: Enable Hebrew-speaking users to configure their accounts and view profiles in Hebrew.
Output: messages/he.json with settings (72 keys) and profile (33 keys) Hebrew translations.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/33-hebrew-translation/33-RESEARCH.md
@messages/en.json
@messages/he.json
</context>

<tasks>

<task type="auto">
  <name>Task 1: Translate settings namespace (72 keys)</name>
  <files>messages/he.json</files>
  <action>
Add Hebrew translations for the entire "settings" namespace to messages/he.json.

Keys to translate:
- settings.title, settings.adminSettings, settings.manageProfile, settings.viewingAs
- settings.tabs.* (4 keys: profile, availability, notifications, preferences)
- settings.profile.* (22 keys: title, personalInfo, firstName, lastName, email, phone, bio, etc.)
- settings.availability.* (16 keys: title, acceptingClients, blockedDates, etc.)
- settings.notifications.* (17 keys including nested categories and options)
- settings.admin.* (2 keys)

Preserve ICU syntax:
- "Viewing as {role}" = "צופה כ{role}"
- "Blocked Dates ({count})" = "תאריכים חסומים ({count})"

Domain-specific terms:
- "Availability" = "זמינות"
- "Accepting New Clients" = "מקבל לקוחות חדשים"
- "Blocked Dates" = "תאריכים חסומים"
- "Notification Preferences" = "העדפות התראות"
  </action>
  <verify>
Run: `node -e "const he=require('./messages/he.json'); console.log('settings keys:', Object.keys(he.settings || {}).length > 0 ? 'EXISTS' : 'MISSING')"`
  </verify>
  <done>settings namespace exists in he.json with all 72 keys translated to Hebrew</done>
</task>

<task type="auto">
  <name>Task 2: Translate profile namespace (33 keys)</name>
  <files>messages/he.json</files>
  <action>
Add Hebrew translations for the entire "profile" namespace to messages/he.json.

Keys to translate:
- profile.title, profile.editProfile, profile.viewPublicProfile, profile.back
- profile.tabs.* (5 keys: posts, reposts, about, reviews, portfolio)
- profile.stats.* (8 keys)
- profile.sections.* (5 keys)
- profile.investor.* (3 keys)
- profile.provider.* (3 keys)
- profile.empty.* (3 keys)
- profile.notFound.* (2 keys)

Domain-specific terms:
- "Public Profile" = "פרופיל ציבורי"
- "Portfolio" = "תיק עבודות"
- "Reviews" = "ביקורות"
- "Followers" = "עוקבים"
- "Following" = "עוקב אחרי"
  </action>
  <verify>
Run: `node -e "const he=require('./messages/he.json'); console.log('profile keys:', Object.keys(he.profile || {}).length > 0 ? 'EXISTS' : 'MISSING')"`
  </verify>
  <done>profile namespace exists in he.json with all 33 keys translated to Hebrew</done>
</task>

</tasks>

<verification>
After both tasks:
```bash
node -e "
const he=require('./messages/he.json');
const en=require('./messages/en.json');
function countKeys(obj) {
  let count = 0;
  for (const key in obj) {
    if (typeof obj[key] === 'object' && !Array.isArray(obj[key])) {
      count += countKeys(obj[key]);
    } else { count++; }
  }
  return count;
}
const settEn = countKeys(en.settings);
const settHe = countKeys(he.settings || {});
const profEn = countKeys(en.profile);
const profHe = countKeys(he.profile || {});
console.log('settings: ' + settHe + '/' + settEn + ' keys');
console.log('profile: ' + profHe + '/' + profEn + ' keys');
console.log(settHe === settEn && profHe === profEn ? 'PASS' : 'INCOMPLETE');
"
```
</verification>

<success_criteria>
- [ ] settings namespace: 72/72 keys translated
- [ ] profile namespace: 33/33 keys translated
- [ ] All ICU patterns preserved
- [ ] No JSON syntax errors
</success_criteria>

<output>
After completion, create `.planning/phases/33-hebrew-translation/33-02-SUMMARY.md`
</output>
