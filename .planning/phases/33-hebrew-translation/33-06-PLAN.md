---
phase: 33-hebrew-translation
plan: 06
type: execute
wave: 1
depends_on: []
files_modified:
  - messages/he.json
autonomous: true

must_haves:
  truths:
    - "User sees Hebrew onboarding role selection options"
    - "User sees Hebrew questionnaire questions and option descriptions"
    - "User sees Hebrew progress indicators and navigation"
  artifacts:
    - path: "messages/he.json"
      provides: "onboarding namespace Hebrew translations"
      contains: "onboarding"
  key_links:
    - from: "messages/he.json"
      to: "useTranslations('onboarding')"
      via: "next-intl message loading"
      pattern: "\"onboarding\""
---

<objective>
Translate onboarding namespace to Hebrew.

Purpose: Enable Hebrew-speaking investors to complete their onboarding questionnaire in Hebrew.
Output: messages/he.json with onboarding namespace (156 keys) fully translated.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/33-hebrew-translation/33-RESEARCH.md
@messages/en.json
@messages/he.json
</context>

<tasks>

<task type="auto">
  <name>Task 1: Translate onboarding core, roles, questionnaire base (30 keys)</name>
  <files>messages/he.json</files>
  <action>
Add Hebrew translations for onboarding core and role sections to messages/he.json.

Keys to translate:
- onboarding.title, onboarding.subtitle, onboarding.settingUp, onboarding.continue
- onboarding.roles.* (8 keys - 4 roles with label and description each)
- onboarding.questionnaire.* (10 keys: title, subtitle, progress, upNext, next, back, skip, complete, saveChanges, error)
- onboarding.options.* (4 keys: bedrooms, area, minimum, maximum)
- onboarding.complete.* (3 keys: title, subtitle, cta)
- onboarding.editProfile.* (6 keys)

Preserve ICU syntax:
- "Step {current} of {total}" = "שלב {current} מתוך {total}"
- "Up next: {step}" = "הבא: {step}"

Domain-specific terms:
- "Investment Questionnaire" = "שאלון השקעות"
- "Complete Your Profile" = "השלם את הפרופיל"
- "Skip for now" = "דלג לעת עתה"
  </action>
  <verify>
Run: `node -e "const he=require('./messages/he.json'); console.log('onboarding.roles:', he.onboarding?.roles ? 'EXISTS' : 'MISSING'); console.log('onboarding.questionnaire:', he.onboarding?.questionnaire ? 'EXISTS' : 'MISSING')"`
  </verify>
  <done>onboarding core, roles, and questionnaire base sections translated</done>
</task>

<task type="auto">
  <name>Task 2: Translate onboarding questions part 1 (65 keys)</name>
  <files>messages/he.json</files>
  <action>
Add Hebrew translations for onboarding.questions first half (citizenship through goals).

Keys to translate:
- onboarding.questions.citizenship.* (4 keys: title, description, options.israeli, options.nonIsraeli)
- onboarding.questions.residency.* (9 keys: title, description, + 4 options with label/description)
- onboarding.questions.experience.* (7 keys)
- onboarding.questions.ownsProperty.* (4 keys)
- onboarding.questions.investmentType.* (6 keys)
- onboarding.questions.budget.* (6 keys)
- onboarding.questions.horizon.* (7 keys)
- onboarding.questions.goals.* (9 keys)

Domain-specific terms:
- "Israeli Citizen" = "אזרח ישראלי"
- "Returning Resident (Toshav Chozer)" = "תושב חוזר"
- "Investment Experience" = "ניסיון בהשקעות"
- "Investment Horizon" = "אופק ההשקעה"
- "Capital Appreciation" = "עליית ערך"
- "Rental Income" = "הכנסה משכירות"
  </action>
  <verify>
Run: `node -e "const he=require('./messages/he.json'); const qs = ['citizenship','residency','experience','ownsProperty','investmentType','budget','horizon','goals']; qs.forEach(q => console.log('questions.' + q + ':', he.onboarding?.questions?.[q] ? 'EXISTS' : 'MISSING'))"`
  </verify>
  <done>First half of onboarding questions translated</done>
</task>

<task type="auto">
  <name>Task 3: Translate onboarding questions part 2 (61 keys)</name>
  <files>messages/he.json</files>
  <action>
Add Hebrew translations for onboarding.questions second half (yield through additional).

Keys to translate:
- onboarding.questions.yield.* (7 keys)
- onboarding.questions.financing.* (7 keys)
- onboarding.questions.propertyTypes.* (2 keys: title, description)
- onboarding.questions.locations.* (17 keys: title, description, + 15 city names)
- onboarding.questions.propertySize.* (6 keys)
- onboarding.questions.amenities.* (2 keys)
- onboarding.questions.timeline.* (9 keys)
- onboarding.questions.services.* (7 keys)
- onboarding.questions.additional.* (4 keys including ICU charCount)

Preserve ICU syntax:
- "{count}/{max} characters" = "{count}/{max} תווים"

City names - keep in Hebrew:
- "Tel Aviv" = "תל אביב"
- "Jerusalem" = "ירושלים"
- "Haifa" = "חיפה"
- etc.

Domain-specific terms:
- "Rental Yield Focus" = "התמקדות בתשואת שכירות"
- "Appreciation Focus" = "התמקדות בעליית ערך"
- "Cash Purchase" = "רכישה במזומן"
- "Mortgage/Financing" = "משכנתא/מימון"
  </action>
  <verify>
Run: `node -e "const he=require('./messages/he.json'); const qs = ['yield','financing','propertyTypes','locations','propertySize','amenities','timeline','services','additional']; qs.forEach(q => console.log('questions.' + q + ':', he.onboarding?.questions?.[q] ? 'EXISTS' : 'MISSING'))"`
  </verify>
  <done>Second half of onboarding questions translated</done>
</task>

</tasks>

<verification>
After all tasks:
```bash
node -e "
const he=require('./messages/he.json');
const en=require('./messages/en.json');
function countKeys(obj) {
  let count = 0;
  for (const key in obj) {
    if (typeof obj[key] === 'object' && !Array.isArray(obj[key])) {
      count += countKeys(obj[key]);
    } else { count++; }
  }
  return count;
}
const onbEn = countKeys(en.onboarding);
const onbHe = countKeys(he.onboarding || {});
console.log('onboarding: ' + onbHe + '/' + onbEn + ' keys');
console.log(onbHe === onbEn ? 'PASS' : 'INCOMPLETE');
"
```
</verification>

<success_criteria>
- [ ] onboarding namespace: 156/156 keys translated
- [ ] All ICU patterns preserved (progress, charCount)
- [ ] Israeli cities in proper Hebrew spelling
- [ ] Question descriptions are natural and helpful in Hebrew
- [ ] No JSON syntax errors
</success_criteria>

<output>
After completion, create `.planning/phases/33-hebrew-translation/33-06-SUMMARY.md`
</output>
