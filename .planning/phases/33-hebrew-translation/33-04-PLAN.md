---
phase: 33-hebrew-translation
plan: 04
type: execute
wave: 1
depends_on: []
files_modified:
  - messages/he.json
autonomous: true

must_haves:
  truths:
    - "User sees Hebrew deal stages (Interest, With Broker, Mortgage, etc.)"
    - "User sees Hebrew deal card labels and provider information"
    - "User sees Hebrew investor questionnaire labels in deal context"
  artifacts:
    - path: "messages/he.json"
      provides: "deals namespace Hebrew translations"
      contains: "deals"
  key_links:
    - from: "messages/he.json"
      to: "useTranslations('deals')"
      via: "next-intl message loading"
      pattern: "\"deals\""
---

<objective>
Translate deals namespace to Hebrew.

Purpose: Enable Hebrew-speaking users to manage their real estate deals in Hebrew.
Output: messages/he.json with deals namespace (204 keys) fully translated.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/33-hebrew-translation/33-RESEARCH.md
@messages/en.json
@messages/he.json
</context>

<tasks>

<task type="auto">
  <name>Task 1: Translate deals core, stages, card, detail, providers (70 keys)</name>
  <files>messages/he.json</files>
  <action>
Add Hebrew translations for deals core sections to messages/he.json.

Keys to translate:
- deals.title, deals.myDeals, deals.activeDeal, deals.activeDeals, deals.completedDeals
- deals.stages.* (7 keys: interest, brokerAssigned, mortgage, legal, closing, completed, cancelled)
- deals.card.* (8 keys including ICU plural providersAssigned)
- deals.detail.* (20 keys)
- deals.providers.* (22 keys)

CRITICAL - Preserve ICU plural syntax:
```json
"providersAssigned": "{count, plural, =1 {ספק אחד} other {# ספקים}} מוקצים"
```

Domain-specific terms:
- "Interest" = "התעניינות"
- "With Broker" = "אצל המתווך"
- "Mortgage" = "משכנתא"
- "Legal Review" = "בדיקה משפטית"
- "Closing" = "סגירה"
- "Deal Value" = "ערך העסקה"
  </action>
  <verify>
Run: `node -e "const he=require('./messages/he.json'); console.log('deals.stages:', he.deals?.stages ? 'EXISTS' : 'MISSING'); console.log('deals.providers:', he.deals?.providers ? 'EXISTS' : 'MISSING')"`
  </verify>
  <done>deals core, stages, card, detail, providers sections translated</done>
</task>

<task type="auto">
  <name>Task 2: Translate deals files, activity, empty, filters (35 keys)</name>
  <files>messages/he.json</files>
  <action>
Add Hebrew translations for deals files, activity, empty, and filters sections.

Keys to translate:
- deals.files.* (25 keys including categories and visibility)
- deals.activity.* (13 keys)
- deals.empty.* (8 keys)
- deals.filters.* (9 keys)

CRITICAL - Preserve ICU plural syntax:
```json
"filesCount": "{count, plural, =1 {קובץ אחד} other {# קבצים}} הועלו"
```

Domain-specific terms:
- "Contract" = "חוזה"
- "ID Document" = "מסמך זיהוי"
- "Financial" = "פיננסי"
- "Legal" = "משפטי"
- "Stage changed" = "השלב השתנה"
- "File uploaded" = "קובץ הועלה"
  </action>
  <verify>
Run: `node -e "const he=require('./messages/he.json'); console.log('deals.files:', he.deals?.files ? 'EXISTS' : 'MISSING'); console.log('deals.filters:', he.deals?.filters ? 'EXISTS' : 'MISSING')"`
  </verify>
  <done>deals files, activity, empty, filters sections translated</done>
</task>

<task type="auto">
  <name>Task 3: Translate deals questionnaire section (99 keys)</name>
  <files>messages/he.json</files>
  <action>
Add Hebrew translations for deals.questionnaire section (investor profile display in deal context).

Keys to translate:
- deals.questionnaire.title, noProfile
- deals.questionnaire.sections.* (4 keys)
- deals.questionnaire.fields.* (16 keys)
- deals.questionnaire.values.* (10 keys including ICU patterns)
- deals.questionnaire.citizenship.* (2 keys)
- deals.questionnaire.residencyStatus.* (4 keys)
- deals.questionnaire.experienceLevel.* (3 keys)
- deals.questionnaire.investmentType.* (2 keys)
- deals.questionnaire.investmentHorizon.* (3 keys)
- deals.questionnaire.yieldPreference.* (3 keys)
- deals.questionnaire.financingApproach.* (3 keys)
- deals.questionnaire.purchaseTimeline.* (4 keys)
- deals.questionnaire.locationFlexibility.* (3 keys)
- deals.questionnaire.goals.* (4 keys)
- deals.questionnaire.services.* (3 keys)
- deals.questionnaire.propertyTypes.* (4 keys)
- deals.questionnaire.amenities.* (12 keys)

Preserve ICU patterns:
- "From {amount}" = "מ-{amount}"
- "{min} - {max}" = "{min} - {max}"
- "{min} - {max} bedrooms" = "{min} - {max} חדרים"

Domain-specific terms (should match common namespace):
- "Israeli Citizen" = "אזרח ישראלי"
- "Returning Resident" = "תושב חוזר"
- "Investment Property" = "נכס להשקעה"
  </action>
  <verify>
Run: `node -e "const he=require('./messages/he.json'); console.log('deals.questionnaire:', he.deals?.questionnaire ? 'EXISTS' : 'MISSING'); console.log('questionnaire sections:', Object.keys(he.deals?.questionnaire || {}).length)"`
  </verify>
  <done>deals.questionnaire section translated with all 99 keys</done>
</task>

</tasks>

<verification>
After all tasks:
```bash
node -e "
const he=require('./messages/he.json');
const en=require('./messages/en.json');
function countKeys(obj) {
  let count = 0;
  for (const key in obj) {
    if (typeof obj[key] === 'object' && !Array.isArray(obj[key])) {
      count += countKeys(obj[key]);
    } else { count++; }
  }
  return count;
}
const dealsEn = countKeys(en.deals);
const dealsHe = countKeys(he.deals || {});
console.log('deals: ' + dealsHe + '/' + dealsEn + ' keys');
console.log(dealsHe === dealsEn ? 'PASS' : 'INCOMPLETE');
"
```
</verification>

<success_criteria>
- [ ] deals namespace: 204/204 keys translated
- [ ] All ICU plural patterns preserved correctly
- [ ] Stage names are clear and natural in Hebrew
- [ ] Questionnaire terms match common namespace where applicable
- [ ] No JSON syntax errors
</success_criteria>

<output>
After completion, create `.planning/phases/33-hebrew-translation/33-04-SUMMARY.md`
</output>
