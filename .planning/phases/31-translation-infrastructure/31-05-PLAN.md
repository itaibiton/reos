---
phase: 31-translation-infrastructure
plan: 05
type: execute
wave: 2
depends_on: ["31-01"]
files_modified:
  - messages/en.json
  - src/app/[locale]/(app)/dashboard/page.tsx
  - src/app/[locale]/(app)/settings/page.tsx
  - src/app/[locale]/(app)/clients/page.tsx
  - src/app/[locale]/(app)/clients/[id]/page.tsx
  - src/components/settings/AvailabilitySettings.tsx
  - src/components/profile/ProfileCompletenessCard.tsx
  - src/components/IncompleteProfileReminder.tsx
autonomous: true

must_haves:
  truths:
    - "Dashboard shows translated labels"
    - "Settings page tabs and forms are translated"
    - "Client pages show translated content"
  artifacts:
    - path: "messages/en.json"
      provides: "dashboard and settings namespaces"
      contains: '"dashboard":'
  key_links:
    - from: "src/app/[locale]/(app)/dashboard/page.tsx"
      to: "messages/en.json"
      via: "useTranslations('dashboard')"
      pattern: "useTranslations.*dashboard"
---

<objective>
Add dashboard, settings, and clients namespaces and translate related pages.

Purpose: Dashboard is the provider entry point, settings manages user preferences, clients shows provider's client list.

Output: dashboard, settings, clients namespaces in en.json, all related pages using useTranslations.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/milestones/v1.4-ROADMAP.md
@.planning/phases/31-translation-infrastructure/31-RESEARCH.md
@messages/en.json
@src/app/[locale]/(app)/dashboard/page.tsx
@src/app/[locale]/(app)/settings/page.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add dashboard, settings, clients namespaces</name>
  <files>messages/en.json</files>
  <action>
Add `dashboard`, `settings`, and `clients` namespaces to messages/en.json.

```json
"dashboard": {
  "title": "Dashboard",
  "welcome": "Welcome back, {name}",
  "welcomeGeneric": "Welcome back",

  "stats": {
    "title": "Overview",
    "activeDeals": "Active Deals",
    "completedDeals": "Completed Deals",
    "totalValue": "Total Deal Value",
    "pendingRequests": "Pending Requests",
    "responseTime": "Avg Response Time",
    "rating": "Rating",
    "clients": "Clients"
  },

  "sections": {
    "recentDeals": "Recent Deals",
    "pendingRequests": "Pending Requests",
    "upcomingTasks": "Upcoming Tasks",
    "monthlyTrends": "Monthly Trends"
  },

  "trends": {
    "title": "Performance Trends",
    "deals": "Deals",
    "revenue": "Revenue",
    "last6Months": "Last 6 months"
  },

  "quickActions": {
    "title": "Quick Actions",
    "browse": "Browse Properties",
    "saved": "Saved Properties",
    "newListing": "New Listing",
    "viewDeals": "View Deals"
  },

  "empty": {
    "noDeals": "No active deals",
    "noRequests": "No pending requests",
    "getStarted": "Get started by browsing properties"
  }
},

"settings": {
  "title": "Settings",

  "tabs": {
    "profile": "Profile",
    "availability": "Availability",
    "notifications": "Notifications",
    "preferences": "Preferences"
  },

  "profile": {
    "title": "Profile Settings",
    "personalInfo": "Personal Information",
    "firstName": "First Name",
    "lastName": "Last Name",
    "email": "Email",
    "phone": "Phone",
    "bio": "Bio",
    "bioPlaceholder": "Tell us about yourself...",
    "specializations": "Specializations",
    "serviceAreas": "Service Areas",
    "languages": "Languages",
    "contactPreference": "Preferred Contact Method",
    "save": "Save Changes",
    "saved": "Changes saved"
  },

  "availability": {
    "title": "Availability Settings",
    "acceptingClients": "Accepting New Clients",
    "acceptingClientsDesc": "When enabled, you'll appear in provider searches and can receive new requests",
    "blockedDates": "Blocked Dates",
    "blockedDatesDesc": "Select dates when you're unavailable",
    "addBlockedDate": "Add Blocked Date",
    "removeDate": "Remove",
    "noBlockedDates": "No blocked dates"
  },

  "notifications": {
    "title": "Notification Preferences",
    "emailNotifications": "Email Notifications",
    "categories": {
      "deals": "Deal Updates",
      "messages": "New Messages",
      "requests": "Service Requests",
      "marketing": "Marketing & Updates"
    },
    "options": {
      "newDeal": "New deal assigned",
      "dealUpdate": "Deal status changes",
      "newMessage": "New chat messages",
      "newRequest": "New service requests",
      "requestUpdate": "Request status changes",
      "newsletter": "Newsletter",
      "tips": "Tips & best practices"
    }
  }
},

"clients": {
  "title": "Clients",
  "myClients": "My Clients",

  "list": {
    "name": "Name",
    "email": "Email",
    "phone": "Phone",
    "activeDeals": "Active Deals",
    "totalDeals": "Total Deals",
    "lastActivity": "Last Activity",
    "viewProfile": "View Profile"
  },

  "detail": {
    "overview": "Overview",
    "deals": "Deals",
    "documents": "Documents",
    "notes": "Notes",
    "contactInfo": "Contact Information",
    "investorProfile": "Investor Profile",
    "dealHistory": "Deal History"
  },

  "empty": {
    "noClients": "No clients yet",
    "getStarted": "Accept service requests to start building your client list"
  }
}
```

Keep existing namespaces intact.
  </action>
  <verify>
Run: `node -e "const m = require('./messages/en.json'); console.log(['dashboard', 'settings', 'clients'].filter(k => m[k]).join(', '))"`
Should output: dashboard, settings, clients
  </verify>
  <done>dashboard, settings, clients namespaces added to en.json</done>
</task>

<task type="auto">
  <name>Task 2: Translate dashboard and settings pages</name>
  <files>
    src/app/[locale]/(app)/dashboard/page.tsx
    src/app/[locale]/(app)/settings/page.tsx
    src/app/[locale]/(app)/analytics/page.tsx
  </files>
  <action>
Update pages to use translations.

**dashboard/page.tsx:**
1. Add `useTranslations("dashboard")` import and hook
2. Replace:
   - "Dashboard" -> `t("title")`
   - "Welcome back" -> `t("welcomeGeneric")` or `t("welcome", { name })`
   - Stat labels -> `t("stats.activeDeals")`, etc.
   - Section titles -> `t("sections.recentDeals")`, etc.
   - Empty states -> `t("empty.noDeals")`, etc.

**settings/page.tsx:**
1. Add `useTranslations("settings")` import and hook
2. Replace:
   - "Settings" -> `t("title")`
   - Tab labels -> `t("tabs.profile")`, `t("tabs.availability")`, etc.
   - Form labels -> `t("profile.firstName")`, etc.
   - Button text -> `t("profile.save")`

**analytics/page.tsx:**
If this page has dashboard-like content, use dashboard namespace or add analytics keys to dashboard namespace.
  </action>
  <verify>
Run: `grep -c "useTranslations" src/app/\[locale\]/\(app\)/dashboard/page.tsx src/app/\[locale\]/\(app\)/settings/page.tsx`
Should show counts for both files
  </verify>
  <done>Dashboard and settings pages use translations</done>
</task>

<task type="auto">
  <name>Task 3: Translate clients pages and settings components</name>
  <files>
    src/app/[locale]/(app)/clients/page.tsx
    src/app/[locale]/(app)/clients/[id]/page.tsx
    src/components/settings/AvailabilitySettings.tsx
    src/components/profile/ProfileCompletenessCard.tsx
    src/components/IncompleteProfileReminder.tsx
  </files>
  <action>
Update client pages and settings components.

**clients/page.tsx:**
1. Add `useTranslations("clients")` import and hook
2. Replace:
   - "Clients" -> `t("title")`
   - Column headers -> `t("list.name")`, `t("list.email")`, etc.
   - Empty state -> `t("empty.noClients")`

**clients/[id]/page.tsx:**
1. Add translations
2. Replace tab labels and section titles

**AvailabilitySettings.tsx:**
1. Add `useTranslations("settings.availability")` import and hook
2. Replace:
   - "Accepting New Clients" -> `t("acceptingClients")`
   - Description text -> `t("acceptingClientsDesc")`
   - "Blocked Dates" -> `t("blockedDates")`
   - Button labels -> `t("addBlockedDate")`, `t("removeDate")`

**ProfileCompletenessCard.tsx:**
1. Add `useTranslations("settings.profile")` or create profile namespace
2. Replace all hardcoded labels

**IncompleteProfileReminder.tsx:**
1. Add translations
2. Replace reminder text and CTA button
  </action>
  <verify>
Run: `grep -l "useTranslations" src/app/\[locale\]/\(app\)/clients/*.tsx src/components/settings/*.tsx src/components/profile/ProfileCompletenessCard.tsx 2>/dev/null | wc -l`
Should be >= 4
  </verify>
  <done>Clients pages and settings components use translations</done>
</task>

</tasks>

<verification>
1. messages/en.json parses without error
2. dashboard, settings, clients namespaces exist
3. All pages compile without TypeScript errors
4. Dev server shows translated content
5. No hardcoded English strings on dashboard, settings, clients pages
</verification>

<success_criteria>
1. Three namespaces exist: dashboard, settings, clients
2. Dashboard page uses useTranslations
3. Settings page and components use useTranslations
4. Clients pages use useTranslations
5. Zero hardcoded user-facing strings on these pages
</success_criteria>

<output>
After completion, create `.planning/phases/31-translation-infrastructure/31-05-SUMMARY.md`
</output>
