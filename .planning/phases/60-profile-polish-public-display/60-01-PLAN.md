# Plan 60-01: Public Profile Enhancement with All Vendor Fields and i18n

## Overview

Add missing vendor fields to public profile (external recommendations, website, custom photo), update provider directory cards/search to use custom photos, and ensure full i18n.

## Changes

### 1. Backend: Return missing fields in getPublicProfile (convex/serviceProviderProfiles.ts)

- Add `externalRecommendations` to return
- Add `websiteUrl` to return
- Resolve `customImageStorageId` → URL via `ctx.storage.getUrl()`
- Return `profilePhotoUrl` (custom photo URL, falls back to user.imageUrl)

### 2. Backend: Add custom photo URL to directory/search queries

All queries that return provider `imageUrl` should prefer custom photo:
- `listByRole` (line ~189)
- `searchProviders` (line ~235)
- `getPublicProfile` (line ~424)
- `getPendingVendors` (line ~682)

Helper function: resolve custom photo for a user.

### 3. Frontend: Public profile page enhancements

- Display `externalRecommendations` in About section (with heading)
- Display `websiteUrl` as a link in Contact Info card
- Use `profilePhotoUrl` for avatar (prefer custom over Clerk photo)

### 4. Frontend: Provider cards + search results

- Update ProviderCardRenderer to use profilePhotoUrl
- Update ProviderDetailModal to use profilePhotoUrl

### 5. i18n (messages/en.json + messages/he.json)

- Add translations for: externalRecommendations section heading, website label
- ~10 keys per language

## Verification
- [ ] `npx tsc --noEmit` passes
- [ ] Public profile shows recommendations, website, custom photo
- [ ] Provider directory cards show custom photo
- [ ] All new fields translated EN + HE
- [ ] RTL layout correct
