---
phase: 53-hero-navigation-mobile
plan: 03
type: execute
wave: 2
depends_on: ["53-01", "53-02"]
files_modified: []
autonomous: false

must_haves:
  truths:
    - "Mobile scroll animation thresholds work correctly"
    - "Hero and Navigation render correctly across all mobile viewports"
    - "No horizontal scrolling on any mobile viewport"
  artifacts: []
  key_links:
    - from: "Navigation.tsx"
      to: "scroll event handler"
      via: "scroll threshold values"
      pattern: "scrollY.*>"
---

<objective>
Verify complete mobile responsiveness of Hero and Navigation with visual verification checkpoint.

Purpose: The scroll animation that adjusts nav padding/border-radius should still work correctly on mobile after responsive changes. Final verification ensures no regressions and all requirements are met.

Output: Verified mobile-responsive Hero and Navigation passing all checkpoint criteria.
</objective>

<execution_context>
@/Users/Kohelet/.claude/get-shit-done/workflows/execute-plan.md
@/Users/Kohelet/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/53-hero-navigation-mobile/53-RESEARCH.md
@src/components/newlanding/Navigation.tsx
@src/components/newlanding/Hero.tsx
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 1: Visual Verification of Mobile Responsiveness</name>
  <what-built>
Complete mobile responsive Hero section and Navigation with hamburger menu:

**Hero changes (53-01):**
- Text sizing: 3xl (mobile) -> 5xl (768px+) -> 7xl (1024px+)
- Stats grid: 1 column (< 640px) -> 2x2 (640px+) -> 4 columns (768px+)
- CTA buttons: stacked (< 640px) -> horizontal (640px+)
- Dashboard mockup: scaled height, hidden sidebar on mobile

**Navigation changes (53-02):**
- Hamburger menu visible on mobile (< 768px)
- Slide-in Sheet from left with smooth animation
- Platform/Solutions as accordion items
- 44px+ touch targets on all menu items
- Menu closes on link click or outside tap
- "Log in" hidden below 640px in header
  </what-built>
  <how-to-verify>
Run `npm run dev` and test at multiple viewports using browser DevTools (toggle device toolbar) or real devices:

**1. Small phone (320px - iPhone SE):**
- [ ] No horizontal scrolling on page
- [ ] Hero heading readable (not cut off)
- [ ] Stats displayed in 1 column (stacked)
- [ ] CTA buttons stacked vertically
- [ ] Hamburger menu icon visible
- [ ] "Log in" NOT visible in header
- [ ] "Get Started" visible in header

**2. Standard phone (375px - iPhone):**
- [ ] All checks from 320px pass
- [ ] Mobile menu opens and closes smoothly
- [ ] Accordion items (Platform, Solutions) work correctly
- [ ] Touch targets feel adequate when tapping

**3. Large phone (414px - iPhone Plus):**
- [ ] Same checks as above pass

**4. Tablet (640px - breakpoint):**
- [ ] Stats in 2x2 grid
- [ ] CTA buttons horizontal
- [ ] "Log in" visible in header
- [ ] Hamburger menu still visible

**5. Tablet/Desktop transition (768px):**
- [ ] Desktop navigation visible
- [ ] Hamburger menu hidden
- [ ] Stats in 4 columns
- [ ] Full hero layout with sidebar

**6. Scroll animation test (any mobile viewport):**
- [ ] Scroll down slowly - nav transitions smoothly to scrolled state
- [ ] Scroll up - nav reverts smoothly to unscrolled state
- [ ] No janky animations or visual glitches
  </how-to-verify>
  <resume-signal>
This is a blocking checkpoint. Execution pauses until you provide feedback.
Type "approved" if all checks pass.
If issues found, describe them specifically (e.g., "At 375px, stats overflow horizontally" or "Accordion animation is choppy").
  </resume-signal>
</task>

</tasks>

<verification>
Human verification checkpoint confirms all mobile responsive requirements met.
</verification>

<success_criteria>
- All 10 Success Criteria from Phase 53 ROADMAP verified:
  1. Hero text and dashboard stack vertically on mobile (< 768px)
  2. Hero heading scales appropriately (text-3xl on mobile, text-5xl on tablet, text-7xl on desktop)
  3. Hero stats display in 2x2 grid on mobile (640px+) and 1 column on small phones (< 640px)
  4. Dashboard mockup scales down and remains readable on mobile
  5. CTA buttons stack vertically on small phones (< 640px)
  6. Navigation shows hamburger menu on mobile (< 768px)
  7. Mobile menu opens with smooth slide-in animation
  8. Platform/Solutions dropdowns become accordion items in mobile menu
  9. Menu items have 44px+ touch targets
  10. "Log in" link hidden below 640px (only "Get Started" visible)
</success_criteria>

<output>
After completion, create `.planning/phases/53-hero-navigation-mobile/53-03-SUMMARY.md`
</output>
