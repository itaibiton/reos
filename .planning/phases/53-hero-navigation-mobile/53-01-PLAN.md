---
phase: 53-hero-navigation-mobile
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/newlanding/Hero.tsx
autonomous: true

must_haves:
  truths:
    - "Hero heading is readable and appropriately sized on all viewports from 320px to 1024px+"
    - "Hero stats are easy to scan with proper grid layout at every breakpoint"
    - "CTA buttons are full-width and tappable on mobile, horizontal on larger screens"
    - "Dashboard mockup remains visible and readable without horizontal scrolling"
  artifacts:
    - path: "src/components/newlanding/Hero.tsx"
      provides: "Mobile-responsive hero section"
      contains: "text-3xl md:text-5xl"
  key_links:
    - from: "Hero.tsx"
      to: "Tailwind breakpoints"
      via: "mobile-first responsive classes"
      pattern: "grid-cols-1.*sm:grid-cols"
---

<objective>
Make the Hero section fully mobile responsive for devices 320px-768px.

Purpose: Hero is the first thing visitors see. On mobile, it must be readable, accessible, and visually appealing with proper text scaling, stacked layouts, and touch-friendly CTAs.

Output: Updated Hero.tsx with mobile-first responsive classes covering text sizing, grid layouts, CTA stacking, and dashboard mockup scaling.
</objective>

<execution_context>
@/Users/Kohelet/.claude/get-shit-done/workflows/execute-plan.md
@/Users/Kohelet/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/53-hero-navigation-mobile/53-RESEARCH.md
@src/components/newlanding/Hero.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Hero Text and Layout Mobile Responsiveness</name>
  <files>src/components/newlanding/Hero.tsx</files>
  <action>
Update Hero.tsx with mobile-first responsive classes:

1. **Heading sizing** (HERO-M02):
   - Change `text-5xl md:text-7xl` to `text-3xl md:text-5xl lg:text-7xl`
   - Note: ROADMAP says "text-3xl on mobile, text-5xl on desktop (768px+)" but lg:text-7xl provides better visual hierarchy on large screens (1024px+) - this is an enhancement
   - Heading should be readable on 320px screens

2. **Badge text** (HERO-M07):
   - Keep `text-xs` for mobile, it's already appropriate
   - Ensure badge container doesn't overflow on small screens

3. **Subheading sizing**:
   - Change `text-lg md:text-xl` to mobile-first sizing if needed
   - Keep max-w-2xl for readability

4. **CTA buttons stacking** (HERO-M06):
   - The existing `flex flex-col sm:flex-row gap-4` is already correct
   - Add `w-full sm:w-auto` to buttons so they're full-width on mobile
   - Ensure min-h-[44px] for touch targets

5. **RotatingText adjustment** (HERO-M03):
   - Reduce mainClassName padding for mobile: `px-2 md:px-3 lg:px-4`
   - Adjust py values: `py-0.5 md:py-1`
   - Keep animation speed as-is (2500ms is reasonable)

6. **Viewport height** (HERO-M08):
   - Change `style={{ minHeight: "400vh" }}` pattern for scroll-based animation
   - This is intentional for scroll animation, keep as-is
   - The header padding `pt-32 pb-20 md:pt-48 md:pb-32` is already responsive
  </action>
  <verify>
1. Run `npm run build` - no TypeScript errors
2. Test at each breakpoint in browser DevTools:
   - 320px: Heading is text-3xl, readable, no horizontal scroll
   - 375px: Same as 320px, verify text fits
   - 414px: Same, may have slightly more room
   - 640px: Heading still text-3xl, CTAs become horizontal
   - 768px: Heading becomes text-5xl
   - 1024px: Heading becomes text-7xl
  </verify>
  <done>
Hero text scales progressively: 3xl on mobile (< 768px), 5xl on tablet (768px+), 7xl on desktop (1024px+).
CTA buttons are full-width and stacked on mobile (< 640px), horizontal on larger screens.
Badge text readable on mobile.
  </done>
</task>

<task type="auto">
  <name>Task 2: Hero Stats Grid Mobile Responsiveness</name>
  <files>src/components/newlanding/Hero.tsx</files>
  <action>
Update stats grid layout for mobile (HERO-M04):

1. **Stats container** (lines ~756-796):
   - Change the grid class from `grid grid-cols-2 md:grid-cols-4 gap-8`
   - To: `grid grid-cols-1 sm:grid-cols-2 md:grid-cols-4 gap-6 sm:gap-8`
   - This gives: 1 column on small phones (< 640px), 2x2 on sm (640px+), 4 columns on md (768px+)
   - Note: ROADMAP says "1x4" for small phones which means 1 column x 4 rows (vertical stack)

2. **Stats text sizing**:
   - Change `text-3xl md:text-4xl` to `text-2xl sm:text-3xl md:text-4xl`
   - Ensures numbers don't overflow on small screens

3. **Apply to BOTH stats divs**:
   - Initial stats (around line 756)
   - Switched stats (around line 777)
   - Both should have identical responsive grid classes

4. **Stats container padding**:
   - Change `px-6` to `px-4 sm:px-6` for smaller horizontal margins on mobile
  </action>
  <verify>
Test at each breakpoint in browser DevTools:
- 320px: Stats in 1 column (stacked vertically), no horizontal scroll
- 375px: Still 1 column
- 414px: Still 1 column
- 640px: Stats in 2x2 grid
- 768px: Stats in 4 columns (horizontal row)
  </verify>
  <done>
Stats grid displays as 1 column on small phones (< 640px), 2x2 on tablets (640px+), 4 columns on desktop (768px+).
Stat values scale down appropriately without truncation.
  </done>
</task>

<task type="auto">
  <name>Task 3: Dashboard Mockup Mobile Scaling</name>
  <files>src/components/newlanding/Hero.tsx</files>
  <action>
Adjust dashboard mockup for mobile readability (HERO-M05, HERO-M01):

1. **Mockup container**:
   - The sticky positioning at `top: calc(50vh - 350px)` works for scroll animation
   - Add responsive horizontal margins: Change `max-w-7xl mx-auto` to include padding
   - Add `px-4 sm:px-6 lg:px-8` to the sticky container

2. **Hide complex elements on mobile**:
   - The sidebar is already `hidden md:flex` - keep this
   - The right detail panel is already `hidden lg:block` - keep this
   - On mobile, only the main dashboard content area is visible

3. **Dashboard mockup inner container**:
   - The MacBook bezel styling works at all sizes
   - Ensure the mockup body `h-[550px]` has mobile consideration:
   - Add `h-[400px] sm:h-[450px] md:h-[550px]` for scaled height on mobile

4. **URL bar text**:
   - Keep the mockup URL bar, it adds authenticity
   - Font is already `text-[10px]` which is fine

5. **Main content area**:
   - Chart bars and table are auto-sizing within their container
   - The responsive scaling happens naturally when container is smaller
   - No explicit changes needed to inner mockup content
  </action>
  <verify>
Test at each breakpoint in browser DevTools:
- 320px: Dashboard shows main content only, no sidebar, no horizontal scroll
- 375px: Same, mockup fully visible and readable
- 414px: Same
- 640px: Mockup has more room, still no sidebar
- 768px: Sidebar appears, full desktop mockup layout
  </verify>
  <done>
Dashboard mockup scales down on mobile with hidden sidebar/detail panel.
Height adjusts for mobile viewports.
Mockup remains readable and centered on all mobile devices.
  </done>
</task>

</tasks>

<verification>
After all tasks complete:

1. Run `npm run build` - ensure no TypeScript errors
2. Run `npm run dev` and test at these viewport widths:
   - 320px (iPhone SE): All content visible, no horizontal scroll
   - 375px (iPhone): Text readable, stats stacked
   - 414px (iPhone Plus): Stats may show 2x2
   - 640px (breakpoint): Stats definitely 2x2, buttons horizontal
   - 768px (breakpoint): Full desktop layout begins
3. Test scroll animation still works on mobile viewports
</verification>

<success_criteria>
- Hero heading scales: 3xl (mobile) -> 5xl (768px+) -> 7xl (1024px+)
- Stats grid: 1 col (< 640px) -> 2x2 (640-767px) -> 4 cols (768px+)
- CTA buttons: stacked full-width (< 640px) -> horizontal (640px+)
- Dashboard mockup: scaled height on mobile, sidebar hidden
- No horizontal scrolling at any mobile viewport
- Build passes without errors
</success_criteria>

<output>
After completion, create `.planning/phases/53-hero-navigation-mobile/53-01-SUMMARY.md`
</output>
