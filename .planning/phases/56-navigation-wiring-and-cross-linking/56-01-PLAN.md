---
phase: 56-navigation-wiring-and-cross-linking
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/newlanding/Navigation.tsx
  - src/components/newlanding/Footer.tsx
  - messages/en.json
  - messages/he.json
autonomous: true

must_haves:
  truths:
    - "Top navigation 'Solutions' dropdown links to all 7 provider service pages (/services/broker, /services/lawyer, etc.)"
    - "'Pricing' is a visible link in the top navigation bar"
    - "Footer links to /privacy, /terms, /contact, /pricing, and /services are real working routes"
    - "All navigation and footer links use locale-aware Link component from @/i18n/navigation"
    - "Mobile navigation links close the menu sheet after navigation"
  artifacts:
    - path: "src/components/newlanding/Navigation.tsx"
      provides: "Updated nav with Solutions dropdown linking to 7 provider pages + Pricing link"
      contains: "Link.*from.*@/i18n/navigation"
    - path: "src/components/newlanding/Footer.tsx"
      provides: "Updated footer with real route links using locale-aware Link"
      contains: "Link.*from.*@/i18n/navigation"
    - path: "messages/en.json"
      provides: "Navigation i18n keys for provider solutions and pricing"
      contains: "landing.navigation.solutions.broker"
    - path: "messages/he.json"
      provides: "Hebrew navigation i18n keys for provider solutions and pricing"
      contains: "landing.navigation.solutions.broker"
  key_links:
    - from: "src/components/newlanding/Navigation.tsx"
      to: "/services/[type]"
      via: "Link href from @/i18n/navigation"
      pattern: "Link.*href.*services"
    - from: "src/components/newlanding/Footer.tsx"
      to: "/privacy, /terms, /contact"
      via: "Link href from @/i18n/navigation"
      pattern: "Link.*href.*/privacy"
---

<objective>
Wire the top navigation and footer to real routes. The Solutions dropdown currently has placeholder items (Residential, Commercial, Industrial with href="#") that must be replaced with links to the 7 provider service pages. Add a "Pricing" direct link to the nav. Update the footer from placeholder hrefs to real routes (/privacy, /terms, /contact, /pricing, /services). Convert all `<a>` tags to locale-aware `Link` from `@/i18n/navigation`. Update i18n keys in both EN and HE for new navigation labels.

Purpose: Make all new pages discoverable through navigation and footer (NAV-01, NAV-02, NAV-03, NAV-07).
Output: Updated Navigation.tsx, Footer.tsx, messages/en.json, messages/he.json.
</objective>

<execution_context>
@/Users/Kohelet/.claude/get-shit-done/workflows/execute-plan.md
@/Users/Kohelet/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@src/components/newlanding/Navigation.tsx
@src/components/newlanding/Footer.tsx
@src/i18n/navigation.ts
@src/i18n/routing.ts
@messages/en.json (landing.navigation and landing.footer sections)
@messages/he.json (landing.navigation and landing.footer sections)
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update Navigation.tsx with provider solutions and pricing link</name>
  <files>
    src/components/newlanding/Navigation.tsx
    messages/en.json
    messages/he.json
  </files>
  <action>
    1. Import `Link` from `@/i18n/navigation` in Navigation.tsx.

    2. **Update i18n keys** - In both `messages/en.json` and `messages/he.json`, replace the existing `landing.navigation.solutions` object (currently has residential/commercial/industrial) with the 7 provider types:
       ```json
       "solutions": {
         "broker": { "title": "Real Estate Brokers", "description": "Connect investors with top properties across Israel" },
         "lawyer": { "title": "Real Estate Lawyers", "description": "Legal expertise for cross-border property transactions" },
         "appraiser": { "title": "Property Appraisers", "description": "Professional property valuations and market analysis" },
         "mortgageAdvisor": { "title": "Mortgage Advisors", "description": "Tailored financing solutions for international investors" },
         "entrepreneur": { "title": "Entrepreneurs & Investors", "description": "Discover and manage real estate investment opportunities" },
         "assetManager": { "title": "Asset Managers", "description": "Portfolio management and optimization tools" },
         "financialAdvisor": { "title": "Financial Advisors", "description": "Comprehensive financial planning for property investments" }
       }
       ```
       Hebrew equivalents:
       ```json
       "solutions": {
         "broker": { "title": "מתווכי נדל\"ן", "description": "חיבור משקיעים עם נכסים מובילים בישראל" },
         "lawyer": { "title": "עורכי דין נדל\"ן", "description": "מומחיות משפטית לעסקאות נדל\"ן בינלאומיות" },
         "appraiser": { "title": "שמאי מקרקעין", "description": "הערכות שווי מקצועיות וניתוח שוק" },
         "mortgageAdvisor": { "title": "יועצי משכנתאות", "description": "פתרונות מימון מותאמים למשקיעים בינלאומיים" },
         "entrepreneur": { "title": "יזמים ומשקיעים", "description": "גילוי וניהול הזדמנויות השקעה בנדל\"ן" },
         "assetManager": { "title": "מנהלי נכסים", "description": "כלים לניהול ומיטוב תיק נכסים" },
         "financialAdvisor": { "title": "יועצים פיננסיים", "description": "תכנון פיננסי מקיף להשקעות בנדל\"ן" }
       }
       ```

    3. Add i18n key `landing.navigation.menu.pricing` with value "Pricing" (EN) / "תמחור" (HE).

    4. **Desktop Solutions dropdown** (lines 196-247): Replace the 3 placeholder `<a href="#">` items (residential/commercial/industrial) with 7 `<Link>` items linking to actual provider pages. Use a grid layout `w-[500px] grid-cols-2` for the 7 items. Each item links to `/services/{type}` using the locale-aware `Link`:
       ```tsx
       <Link
         href="/services/broker"
         className="block select-none space-y-1 rounded-md p-3 leading-none no-underline outline-none transition-colors text-white"
       >
       ```
       Use appropriate Lucide icons for each provider type:
       - broker: Building2
       - lawyer: Shield (already imported)
       - appraiser: FileCode (already imported)
       - mortgage-advisor: Home (already imported)
       - entrepreneur: Users (already imported)
       - asset-manager: Building (already imported)
       - financial-advisor: Factory → replace with Briefcase or similar (import Briefcase from lucide-react)

    5. **Replace "Institutions" link** with a "Pricing" link: Change the first simple link (line 250-257) from `<a href="#">{t("menu.institutions")}` to `<Link href="/pricing">{t("menu.pricing")}</Link>`. Keep the same styling classes.

    6. **Remove or repurpose "Developers" link** (line 259-266): Remove this link entirely since there's no developers page. Or replace with a "Contact" link: `<Link href="/contact">{t("menu.contact")}</Link>`. If keeping as contact, add i18n key `landing.navigation.menu.contact` = "Contact" / "צור קשר".

    7. **Desktop Login/Get Started links** (lines 272-276): Convert the `<a href="#">` tags to:
       - Login: `<Link href="/sign-in">`
       - Get Started: `<Link href="/sign-up">`

    8. **Mobile Solutions accordion** (lines 361-394): Replace the 3 placeholder `<a href="#">` items with 7 items matching the desktop Solutions dropdown. Use `<Link>` with `onClick={() => setMobileMenuOpen(false)}` to close menu after navigation.

    9. **Mobile Platform accordion** (lines 332-358): Convert `<a href="#">` to `<Link>` with proper href targets (keep `#` for in-page links if they refer to landing page sections, or link to relevant pages). Since Platform items are generic features, keep them as hash links or remove. If keeping, leave as `<a>` tags.

    10. **Mobile simple links** (lines 398-413): Replace "Institutions" with "Pricing" → `<Link href="/pricing" onClick={() => setMobileMenuOpen(false)}>`. Replace "Developers" with "Contact" → `<Link href="/contact" onClick={() => setMobileMenuOpen(false)}>`.

    11. **Mobile Login/Get Started** (lines 416-431): Convert to `<Link href="/sign-in">` and `<Link href="/sign-up">` with `onClick={() => setMobileMenuOpen(false)}`.

    NOTE: When using `<Link>` from `@/i18n/navigation` with `onClick`, the onClick handler works normally. The Link component handles locale prefixing automatically - do NOT manually prefix with `/${locale}/`.
  </action>
  <verify>
    Run `npx next build 2>&1 | tail -20` to confirm no build errors.
    Grep Navigation.tsx for `from.*@/i18n/navigation` to confirm Link import exists.
    Grep Navigation.tsx to confirm no remaining `href="#"` on solution links.
    Check that `<a href=` appears only for external links or in-page hash anchors, not for route navigation.
  </verify>
  <done>
    Solutions dropdown shows 7 provider types linking to /services/{type}.
    "Pricing" link is visible in top nav linking to /pricing.
    Mobile nav mirrors desktop nav with proper close handlers.
    All route links use locale-aware Link component.
    EN and HE translations exist for all new navigation keys.
  </done>
</task>

<task type="auto">
  <name>Task 2: Update Footer.tsx with real routes and locale-aware Links</name>
  <files>
    src/components/newlanding/Footer.tsx
    messages/en.json
    messages/he.json
  </files>
  <action>
    1. Import `Link` from `@/i18n/navigation` in Footer.tsx. Remove any existing `import Link from 'next/link'` if present (there isn't one currently, but check).

    2. **Update footer link data arrays** — replace `href: "#"` placeholders with real routes:

       **productLinks** (lines 21-26): Update to reference actual pages:
       ```typescript
       const productLinks = [
         { label: t("product.pricing"), href: "/pricing" },
         { label: t("product.services"), href: "/services" },
       ];
       ```
       This replaces the 4 placeholder items (Platform, Data API, Workflows, Security) with 2 real links.

       **solutionsLinks** (lines 28-32): Update to link to provider pages. Pick the top 4 most relevant:
       ```typescript
       const solutionsLinks = [
         { label: t("solutions.brokers"), href: "/services/broker" },
         { label: t("solutions.lawyers"), href: "/services/lawyer" },
         { label: t("solutions.mortgageAdvisors"), href: "/services/mortgage-advisor" },
         { label: t("solutions.allServices"), href: "/services" },
       ];
       ```

       **companyLinks** (lines 34-39): Remove non-existent pages (about, careers, blog), keep contact:
       ```typescript
       const companyLinks = [
         { label: t("company.contact"), href: "/contact" },
       ];
       ```

       **legalLinks** (lines 41-45): Update hrefs to real routes (remove SLA which doesn't exist):
       ```typescript
       const legalLinks = [
         { label: t("legal.privacy"), href: "/privacy" },
         { label: t("legal.terms"), href: "/terms" },
       ];
       ```

    3. **Convert all `<a>` tags to `<Link>`** in the link rendering loops (lines 72-78, 84-90, 98-105, 110-118). Replace:
       ```tsx
       <a href={link.href} className="hover:text-foreground transition-colors">
       ```
       with:
       ```tsx
       <Link href={link.href} className="hover:text-foreground transition-colors">
       ```

    4. **Logo link**: Convert the logo `<a href="#">` (line 58) to `<Link href="/">`.

    5. **Update i18n keys** in both messages/en.json and messages/he.json `landing.footer` section:
       - Add `product.pricing` = "Pricing" / "תמחור"
       - Add `product.services` = "Services" / "שירותים"
       - Replace `solutions.residential/commercial/industrial` with:
         - `solutions.brokers` = "Real Estate Brokers" / "מתווכי נדל\"ן"
         - `solutions.lawyers` = "Real Estate Lawyers" / "עורכי דין נדל\"ן"
         - `solutions.mortgageAdvisors` = "Mortgage Advisors" / "יועצי משכנתאות"
         - `solutions.allServices` = "All Services" / "כל השירותים"
       - Remove keys that no longer exist: `product.platform`, `product.dataApi`, `product.workflows`, `product.security`, `company.about`, `company.careers`, `company.blog`, `legal.sla`, `solutions.residential`, `solutions.commercial`, `solutions.industrial`

    6. Keep social media links as `<a>` tags since they are external links (correct behavior).
  </action>
  <verify>
    Run `npx next build 2>&1 | tail -20` to confirm no build errors.
    Grep Footer.tsx for `from.*@/i18n/navigation` to confirm Link import.
    Grep Footer.tsx for `href="#"` - should only appear on social icons (external) or not at all.
    Verify no `import Link from 'next/link'` in Footer.tsx.
  </verify>
  <done>
    Footer links to /privacy, /terms, /contact work with locale-aware Link.
    Footer Solutions section links to real provider pages.
    No dead links (removed about, careers, blog, SLA).
    EN and HE translations updated for footer link labels.
  </done>
</task>

</tasks>

<verification>
1. `npx next build` completes without errors
2. Navigation Solutions dropdown contains 7 provider type links
3. "Pricing" link visible in top nav
4. Footer links point to real routes, no `href="#"` on internal links
5. All internal links use `Link` from `@/i18n/navigation`
6. Mobile navigation closes sheet on link click
7. Both EN and HE translation keys exist for all new labels
</verification>

<success_criteria>
- Top navigation includes "Pricing" link (NAV-01)
- Solutions dropdown links to all 7 provider service pages (NAV-02)
- Footer links to /privacy, /terms, /contact, /pricing, /services are working (NAV-03)
- All navigation and footer links use locale-aware Link component (NAV-07)
- Build passes with no errors
</success_criteria>

<output>
After completion, create `.planning/phases/56-navigation-wiring-and-cross-linking/56-01-SUMMARY.md`
</output>
