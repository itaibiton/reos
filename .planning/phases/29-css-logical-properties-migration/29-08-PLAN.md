---
phase: 29-css-logical-properties-migration
plan: 08
type: execute
wave: 2
depends_on: ["29-01", "29-02", "29-03"]
files_modified:
  - src/components/feed/RepostCard.tsx
  - src/components/feed/RepostDialog.tsx
  - src/components/feed/PropertySelector.tsx
  - src/components/feed/PropertyPostCard.tsx
  - src/components/feed/FollowListDialog.tsx
  - src/components/profile/ProviderProfileForm.tsx
  - src/components/profile/InvestorProfileForm.tsx
  - src/components/profile/PortfolioSection.tsx
  - src/components/deals/RequestProviderDialog.tsx
  - src/components/deals/FileUpload.tsx
  - src/components/settings/NotificationSettings.tsx
  - src/components/notifications/NotificationCenter.tsx
  - src/components/landing/ProcessSteps.tsx
autonomous: true

must_haves:
  truths:
    - "Feed posts and cards display correctly in RTL"
    - "Profile forms maintain correct layout in RTL"
    - "Deal management dialogs work correctly in RTL"
    - "Notification center positions correctly in RTL"
  artifacts:
    - path: "src/components/feed/RepostCard.tsx"
      provides: "RTL-compatible feed card"
      contains: "ms-|me-|ps-|pe-"
    - path: "src/components/profile/InvestorProfileForm.tsx"
      provides: "RTL-compatible profile form"
      contains: "ms-|me-|ps-|pe-"
  key_links:
    - from: "src/components/feed/RepostCard.tsx"
      to: "src/components/feed/RepostDialog.tsx"
      via: "Repost action"
      pattern: "RepostDialog"
---

<objective>
Migrate feed, profile, deals, settings, and landing components to CSS logical properties

Purpose: Complete the migration of remaining application components. This includes social feed, user profiles, deal management, settings, and landing page components.

Output: All remaining application components use logical CSS properties.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/29-css-logical-properties-migration/29-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Migrate feed and profile components</name>
  <files>
    src/components/feed/RepostCard.tsx
    src/components/feed/RepostDialog.tsx
    src/components/feed/PropertySelector.tsx
    src/components/feed/PropertyPostCard.tsx
    src/components/feed/FollowListDialog.tsx
    src/components/profile/ProviderProfileForm.tsx
    src/components/profile/InvestorProfileForm.tsx
    src/components/profile/PortfolioSection.tsx
  </files>
  <action>
Migrate RepostCard.tsx (4 directional classes):
- Convert `ml-`/`mr-` to `ms-`/`me-` for author info spacing
- Convert `pl-`/`pr-` to `ps-`/`pe-` for content padding

Migrate RepostDialog.tsx (4 directional classes):
- Convert `ml-`/`mr-` to `ms-`/`me-` for dialog content spacing
- Convert any positioning classes to logical

Migrate PropertySelector.tsx (4 directional classes):
- Convert `ml-`/`mr-` to `ms-`/`me-` for selector item spacing
- Convert `pl-`/`pr-` to `ps-`/`pe-` for list padding

Migrate PropertyPostCard.tsx (1 directional class):
- Convert any directional classes to logical

Migrate FollowListDialog.tsx (1 directional class):
- Convert `ml-`/`mr-` to `ms-`/`me-` for list item spacing

Migrate ProviderProfileForm.tsx (1 directional class):
- Convert any form spacing classes to logical

Migrate InvestorProfileForm.tsx (3 directional classes):
- Convert `ml-`/`mr-` to `ms-`/`me-` for field spacing
- Convert `pl-`/`pr-` to `ps-`/`pe-` for section padding
- Add `rtl:space-x-reverse` if using horizontal space-x-*

Migrate PortfolioSection.tsx (1 directional class):
- Convert any directional classes to logical
  </action>
  <verify>
Run `npm run build` - no TypeScript errors.
Grep check:
`grep -E "(ml-|mr-|pl-|pr-|left-[0-9]|right-[0-9])" src/components/feed/*.tsx src/components/profile/*.tsx`
Should return empty.
  </verify>
  <done>
Feed components use logical properties.
Profile forms use logical spacing.
  </done>
</task>

<task type="auto">
  <name>Task 2: Migrate deals, settings, notifications, and landing components</name>
  <files>
    src/components/deals/RequestProviderDialog.tsx
    src/components/deals/FileUpload.tsx
    src/components/settings/NotificationSettings.tsx
    src/components/notifications/NotificationCenter.tsx
    src/components/landing/ProcessSteps.tsx
  </files>
  <action>
Migrate RequestProviderDialog.tsx (3 directional classes):
- Convert `ml-`/`mr-` to `ms-`/`me-` for dialog content spacing
- Convert `pl-`/`pr-` to `ps-`/`pe-` for form padding

Migrate FileUpload.tsx (2 directional classes):
- Convert `ml-`/`mr-` to `ms-`/`me-` for file item spacing
- Convert any icon positioning to logical

Migrate NotificationSettings.tsx (3 directional classes):
- Convert `ml-`/`mr-` to `ms-`/`me-` for toggle/label spacing
- Convert `pl-`/`pr-` to `ps-`/`pe-` for section padding
- Convert any `border-l-`/`border-r-` to `border-s-`/`border-e-`

Migrate NotificationCenter.tsx (4 directional classes):
- Convert `ml-`/`mr-` to `ms-`/`me-` for notification item spacing
- Convert positioning classes for dropdown to logical
- Add `rtl:space-x-reverse` if using space-x-*

Migrate ProcessSteps.tsx (4 directional classes):
- Convert `ml-`/`mr-` to `ms-`/`me-` for step number/text spacing
- Convert any connector line positioning to logical
- This is landing page component - important for first impression
  </action>
  <verify>
Run `npm run build` - no TypeScript errors.
Grep check:
`grep -E "(ml-|mr-|pl-|pr-|left-[0-9]|right-[0-9]|border-l-|border-r-)" src/components/deals/*.tsx src/components/settings/*.tsx src/components/notifications/*.tsx src/components/landing/*.tsx`
Should return empty.
  </verify>
  <done>
Deal dialogs use logical spacing.
NotificationSettings toggle groups use logical borders.
NotificationCenter dropdown positions correctly.
ProcessSteps landing component works in RTL.
  </done>
</task>

</tasks>

<verification>
1. Build passes: `npm run build`
2. No physical directional classes in remaining components
3. Feed, profile, deals, and settings pages render correctly in RTL mode
</verification>

<success_criteria>
- [ ] RepostCard and feed components migrated
- [ ] Profile forms migrated
- [ ] Deal dialogs migrated
- [ ] NotificationSettings and NotificationCenter migrated
- [ ] ProcessSteps landing component migrated
- [ ] Build passes with no errors
</success_criteria>

<output>
After completion, create `.planning/phases/29-css-logical-properties-migration/29-08-SUMMARY.md`
</output>
