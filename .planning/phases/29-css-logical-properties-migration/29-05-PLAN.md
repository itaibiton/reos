---
phase: 29-css-logical-properties-migration
plan: 05
type: execute
wave: 2
depends_on: ["29-01", "29-02", "29-03"]
files_modified:
  - src/components/dashboard/InvestorDashboard.tsx
  - src/components/dashboard/ProviderDashboard.tsx
  - src/components/dashboard/RecommendedProperties.tsx
  - src/components/properties/PropertyCard.tsx
  - src/components/properties/PropertyImageCarousel.tsx
  - src/components/properties/PropertyMap.tsx
  - src/components/properties/PropertyMapClient.tsx
  - src/components/properties/PropertyForm.tsx
  - src/components/properties/MortgageCalculator.tsx
  - src/components/properties/ValueHistoryChart.tsx
  - src/components/properties/SoldPropertiesTable.tsx
  - src/components/properties/SaveButton.tsx
autonomous: true

must_haves:
  truths:
    - "Dashboard layouts mirror correctly in RTL mode"
    - "Property cards display correctly in RTL"
    - "Property image carousel navigation works in RTL"
    - "Property forms maintain correct layout in RTL"
  artifacts:
    - path: "src/components/dashboard/ProviderDashboard.tsx"
      provides: "RTL-compatible provider dashboard"
      contains: "ms-|me-|ps-|pe-|start-|end-"
    - path: "src/components/properties/PropertyImageCarousel.tsx"
      provides: "RTL-compatible carousel"
      contains: "start-|end-"
  key_links:
    - from: "src/components/dashboard/ProviderDashboard.tsx"
      to: "src/components/properties/PropertyCard.tsx"
      via: "Card composition"
      pattern: "PropertyCard"
---

<objective>
Migrate dashboard and property components to CSS logical properties

Purpose: Dashboard and property components are core to the application. Provider and investor dashboards, property listings, and forms must display correctly in RTL for usability.

Output: All dashboard and property components use logical CSS properties.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/29-css-logical-properties-migration/29-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Migrate dashboard components</name>
  <files>
    src/components/dashboard/InvestorDashboard.tsx
    src/components/dashboard/ProviderDashboard.tsx
    src/components/dashboard/RecommendedProperties.tsx
  </files>
  <action>
Migrate InvestorDashboard.tsx (11 directional classes):
- Convert all `ml-`/`mr-` to `ms-`/`me-` for stat card and content spacing
- Convert all `pl-`/`pr-` to `ps-`/`pe-` for section padding
- Convert `left-`/`right-` positioning to `start-`/`end-` if any
- Add `rtl:space-x-reverse` to horizontal flex containers with `space-x-*`

Migrate ProviderDashboard.tsx (16 directional classes - highest in dashboard):
- Convert all `ml-`/`mr-` to `ms-`/`me-` for metric cards, stat rows
- Convert all `pl-`/`pr-` to `ps-`/`pe-` for content sections
- Convert `left-`/`right-` positioning to `start-`/`end-`
- Pay special attention to chart labels and metric indicators
- Add `rtl:space-x-reverse` where needed

Migrate RecommendedProperties.tsx (4 directional classes):
- Convert `ml-`/`mr-` to `ms-`/`me-` for card spacing
- Convert `pl-`/`pr-` to `ps-`/`pe-` for content padding
  </action>
  <verify>
Run `npm run build` - no TypeScript errors.
Grep check:
`grep -E "(ml-|mr-|pl-|pr-|left-[0-9]|right-[0-9])" src/components/dashboard/*.tsx`
Should return empty.
  </verify>
  <done>
InvestorDashboard uses logical properties throughout.
ProviderDashboard (16 classes) fully migrated to logical properties.
RecommendedProperties uses logical margins.
  </done>
</task>

<task type="auto">
  <name>Task 2: Migrate property components</name>
  <files>
    src/components/properties/PropertyCard.tsx
    src/components/properties/PropertyImageCarousel.tsx
    src/components/properties/PropertyMap.tsx
    src/components/properties/PropertyMapClient.tsx
    src/components/properties/PropertyForm.tsx
    src/components/properties/MortgageCalculator.tsx
    src/components/properties/ValueHistoryChart.tsx
    src/components/properties/SoldPropertiesTable.tsx
    src/components/properties/SaveButton.tsx
  </files>
  <action>
Migrate PropertyCard.tsx (2 directional classes):
- Convert `ml-`/`mr-` to `ms-`/`me-` for badge/price spacing

Migrate PropertyImageCarousel.tsx (7 directional classes):
- Convert `left-*`/`right-*` nav button positioning to `start-*`/`end-*`
- Convert `ml-`/`mr-` to `ms-`/`me-` for thumbnail spacing

Migrate PropertyMap.tsx (3 directional classes):
- Convert any positioning classes to logical

Migrate PropertyMapClient.tsx (1 directional class):
- Convert any positioning or margin classes to logical

Migrate PropertyForm.tsx (3 directional classes):
- Convert `ml-`/`mr-` to `ms-`/`me-` for field spacing
- Convert `pl-`/`pr-` to `ps-`/`pe-` for input padding

Migrate MortgageCalculator.tsx (4 directional classes):
- Convert `ml-`/`mr-` to `ms-`/`me-` for result spacing
- Convert `pl-`/`pr-` to `ps-`/`pe-` for section padding

Migrate ValueHistoryChart.tsx (1 directional class):
- Convert any directional classes to logical

Migrate SoldPropertiesTable.tsx (7 directional classes):
- Convert `text-left`/`text-right` to `text-start`/`text-end`
- Convert `pl-`/`pr-` to `ps-`/`pe-` for cell padding

Migrate SaveButton.tsx (1 directional class):
- Convert any positioning classes to logical
  </action>
  <verify>
Run `npm run build` - no TypeScript errors.
Grep check:
`grep -E "(ml-|mr-|pl-|pr-|left-[0-9]|right-[0-9]|text-left|text-right)" src/components/properties/*.tsx`
Should return empty.
  </verify>
  <done>
PropertyCard uses logical spacing.
PropertyImageCarousel nav buttons use start/end positioning.
PropertyForm and calculator use logical padding.
SoldPropertiesTable uses text-start/end alignment.
  </done>
</task>

</tasks>

<verification>
1. Build passes: `npm run build`
2. No physical directional classes in dashboard or property components
3. Dashboard and property pages render correctly in RTL mode
</verification>

<success_criteria>
- [ ] InvestorDashboard (11 classes) fully migrated
- [ ] ProviderDashboard (16 classes) fully migrated
- [ ] PropertyImageCarousel (7 classes) fully migrated
- [ ] SoldPropertiesTable (7 classes) fully migrated
- [ ] All property components migrated
- [ ] Build passes with no errors
</success_criteria>

<output>
After completion, create `.planning/phases/29-css-logical-properties-migration/29-05-SUMMARY.md`
</output>
