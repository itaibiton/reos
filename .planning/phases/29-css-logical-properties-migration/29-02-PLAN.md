---
phase: 29-css-logical-properties-migration
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/ui/dialog.tsx
  - src/components/ui/alert-dialog.tsx
  - src/components/ui/drawer.tsx
  - src/components/ui/navigation-menu.tsx
  - src/components/ui/select.tsx
  - src/components/ui/table.tsx
  - src/components/ui/calendar.tsx
  - src/components/ui/carousel.tsx
  - src/components/ui/pagination.tsx
autonomous: true

must_haves:
  truths:
    - "Dialog content aligns correctly in both LTR and RTL"
    - "Navigation menu items have correct start/end spacing"
    - "Table text alignment respects document direction"
    - "Calendar navigation arrows position correctly in RTL"
    - "Carousel navigation buttons position correctly in RTL"
  artifacts:
    - path: "src/components/ui/dialog.tsx"
      provides: "RTL-compatible dialog positioning"
      contains: "start-|end-|text-start"
    - path: "src/components/ui/table.tsx"
      provides: "RTL-compatible table alignment"
      contains: "text-start|pe-"
    - path: "src/components/ui/calendar.tsx"
      provides: "RTL-compatible calendar spacing"
      contains: "ps-|pe-|ms-|me-"
  key_links:
    - from: "src/components/ui/dialog.tsx"
      to: "src/components/ui/alert-dialog.tsx"
      via: "Shared positioning pattern"
      pattern: "left-\\[50%\\]|start-\\[50%\\]"
---

<objective>
Migrate medium-priority Shadcn UI components to CSS logical properties

Purpose: These components are used across the application but are less critical than layout/menu components. Migrating them ensures consistent RTL behavior in dialogs, tables, navigation, and interactive elements.

Output: All medium-priority Shadcn components use logical CSS properties.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/29-css-logical-properties-migration/29-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Migrate Dialog, Alert Dialog, Drawer, Navigation Menu</name>
  <files>
    src/components/ui/dialog.tsx
    src/components/ui/alert-dialog.tsx
    src/components/ui/drawer.tsx
    src/components/ui/navigation-menu.tsx
  </files>
  <action>
Migrate dialog.tsx (3 directional classes):
- Find `left-[50%]` - this is centering transform, keep as-is (transform-based centering is physical)
- Find `text-left` -> convert to `text-start` in DialogHeader
- Find close button `right-4` -> convert to `end-4`

Migrate alert-dialog.tsx (2 directional classes):
- Same pattern as dialog: `left-[50%]` keep, `text-left` -> `text-start`

Migrate drawer.tsx (3 directional classes):
- Find `left-0`/`right-0` positioning -> convert to `start-0`/`end-0`
- Find `border-l`/`border-r` -> convert to `border-s`/`border-e`
- Note: Like Sheet, handle direction variants based on side prop

Migrate navigation-menu.tsx (3 directional classes):
- Find `left-0` -> convert to `start-0` in NavigationMenuViewport
- Find `ml-1` -> convert to `ms-1` in NavigationMenuListItem
- Animation classes (slide-in-from-*) keep as-is for RTL-05
  </action>
  <verify>
Run `npm run build` - no TypeScript errors.
Grep check:
`grep -E "(text-left|text-right|ml-[0-9]|mr-[0-9]|border-l-|border-r-)" src/components/ui/dialog.tsx src/components/ui/alert-dialog.tsx src/components/ui/drawer.tsx src/components/ui/navigation-menu.tsx`
Should return empty (no matches except animation classes).
  </verify>
  <done>
Dialog components use text-start for content alignment.
Drawer uses logical positioning for side variants.
Navigation menu uses logical margins.
  </done>
</task>

<task type="auto">
  <name>Task 2: Migrate Select, Table, Calendar, Carousel, Pagination</name>
  <files>
    src/components/ui/select.tsx
    src/components/ui/table.tsx
    src/components/ui/calendar.tsx
    src/components/ui/carousel.tsx
    src/components/ui/pagination.tsx
  </files>
  <action>
Migrate select.tsx (4 directional classes):
- Find `left-2`/`right-2` indicator positioning -> convert to `start-2`/`end-2`
- Find any `pl-`/`pr-` -> convert to `ps-`/`pe-`

Migrate table.tsx (2 directional classes):
- Find `text-left` in TableHead -> convert to `text-start`
- Find `pr-0` -> convert to `pe-0`

Migrate calendar.tsx (8 directional classes):
- Find all `pl-`/`pr-` patterns -> convert to `ps-`/`pe-`
- These are typically in day cells and navigation areas
- Check for `ml-`/`mr-` -> convert to `ms-`/`me-`

Migrate carousel.tsx (6 directional classes):
- Find `left-*`/`right-*` for button positioning -> convert to `start-*`/`end-*`
- Find `ml-`/`mr-` in button containers -> convert to `ms-`/`me-`
- Button positions should be start/end not left/right

Migrate pagination.tsx (2 directional classes):
- Find `pl-2.5`/`pr-2.5` -> convert to `ps-2.5`/`pe-2.5`
  </action>
  <verify>
Run `npm run build` - no TypeScript errors.
Grep check:
`grep -E "(pl-|pr-|left-[0-9]|right-[0-9]|text-left|text-right|ml-|mr-)" src/components/ui/select.tsx src/components/ui/table.tsx src/components/ui/calendar.tsx src/components/ui/carousel.tsx src/components/ui/pagination.tsx`
Should return empty (no matches).
  </verify>
  <done>
Select component uses logical positioning for icons.
Table uses text-start for header alignment.
Calendar uses logical padding throughout.
Carousel navigation buttons use logical positioning.
Pagination uses logical padding.
  </done>
</task>

</tasks>

<verification>
1. Build passes: `npm run build`
2. No physical directional classes remain in migrated files (except centering transforms)
3. Manual test: Tables, dialogs, calendars render correctly in RTL mode
</verification>

<success_criteria>
- [ ] dialog.tsx uses text-start and logical positioning
- [ ] alert-dialog.tsx uses text-start
- [ ] drawer.tsx uses logical positioning
- [ ] navigation-menu.tsx uses logical margins
- [ ] select.tsx uses logical positioning
- [ ] table.tsx uses text-start
- [ ] calendar.tsx uses logical padding
- [ ] carousel.tsx uses logical positioning for buttons
- [ ] pagination.tsx uses logical padding
- [ ] Build passes with no errors
</success_criteria>

<output>
After completion, create `.planning/phases/29-css-logical-properties-migration/29-02-SUMMARY.md`
</output>
